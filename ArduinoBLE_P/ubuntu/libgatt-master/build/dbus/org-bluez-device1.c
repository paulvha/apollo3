/*
 * This file is generated by gdbus-codegen, do not modify it.
 *
 * The license of this code is the same as for the D-Bus interface description
 * it was derived from. Note that it links to GLib, so must comply with the
 * LGPL linking clauses.
 */

#ifdef HAVE_CONFIG_H
#  include "config.h"
#endif

#include "/home/paul/Documents/libgatt-master/build/dbus/org-bluez-device1.h"

#include <string.h>
#ifdef G_OS_UNIX
#  include <gio/gunixfdlist.h>
#endif

typedef struct
{
  GDBusArgInfo parent_struct;
  gboolean use_gvariant;
} _ExtendedGDBusArgInfo;

typedef struct
{
  GDBusMethodInfo parent_struct;
  const gchar *signal_name;
  gboolean pass_fdlist;
} _ExtendedGDBusMethodInfo;

typedef struct
{
  GDBusSignalInfo parent_struct;
  const gchar *signal_name;
} _ExtendedGDBusSignalInfo;

typedef struct
{
  GDBusPropertyInfo parent_struct;
  const gchar *hyphen_name;
  guint use_gvariant : 1;
  guint emits_changed_signal : 1;
} _ExtendedGDBusPropertyInfo;

typedef struct
{
  GDBusInterfaceInfo parent_struct;
  const gchar *hyphen_name;
} _ExtendedGDBusInterfaceInfo;

typedef struct
{
  const _ExtendedGDBusPropertyInfo *info;
  guint prop_id;
  GValue orig_value; /* the value before the change */
} ChangedProperty;

static void
_changed_property_free (ChangedProperty *data)
{
  g_value_unset (&data->orig_value);
  g_free (data);
}

static gboolean
_g_strv_equal0 (gchar **a, gchar **b)
{
  gboolean ret = FALSE;
  guint n;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  if (g_strv_length (a) != g_strv_length (b))
    goto out;
  for (n = 0; a[n] != NULL; n++)
    if (g_strcmp0 (a[n], b[n]) != 0)
      goto out;
  ret = TRUE;
out:
  return ret;
}

static gboolean
_g_variant_equal0 (GVariant *a, GVariant *b)
{
  gboolean ret = FALSE;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  ret = g_variant_equal (a, b);
out:
  return ret;
}

G_GNUC_UNUSED static gboolean
_g_value_equal (const GValue *a, const GValue *b)
{
  gboolean ret = FALSE;
  g_assert (G_VALUE_TYPE (a) == G_VALUE_TYPE (b));
  switch (G_VALUE_TYPE (a))
    {
      case G_TYPE_BOOLEAN:
        ret = (g_value_get_boolean (a) == g_value_get_boolean (b));
        break;
      case G_TYPE_UCHAR:
        ret = (g_value_get_uchar (a) == g_value_get_uchar (b));
        break;
      case G_TYPE_INT:
        ret = (g_value_get_int (a) == g_value_get_int (b));
        break;
      case G_TYPE_UINT:
        ret = (g_value_get_uint (a) == g_value_get_uint (b));
        break;
      case G_TYPE_INT64:
        ret = (g_value_get_int64 (a) == g_value_get_int64 (b));
        break;
      case G_TYPE_UINT64:
        ret = (g_value_get_uint64 (a) == g_value_get_uint64 (b));
        break;
      case G_TYPE_DOUBLE:
        {
          /* Avoid -Wfloat-equal warnings by doing a direct bit compare */
          gdouble da = g_value_get_double (a);
          gdouble db = g_value_get_double (b);
          ret = memcmp (&da, &db, sizeof (gdouble)) == 0;
        }
        break;
      case G_TYPE_STRING:
        ret = (g_strcmp0 (g_value_get_string (a), g_value_get_string (b)) == 0);
        break;
      case G_TYPE_VARIANT:
        ret = _g_variant_equal0 (g_value_get_variant (a), g_value_get_variant (b));
        break;
      default:
        if (G_VALUE_TYPE (a) == G_TYPE_STRV)
          ret = _g_strv_equal0 (g_value_get_boxed (a), g_value_get_boxed (b));
        else
          g_critical ("_g_value_equal() does not handle type %s", g_type_name (G_VALUE_TYPE (a)));
        break;
    }
  return ret;
}

/* ------------------------------------------------------------------------
 * Code for interface org.bluez.Device1
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:OrgBluezDevice1
 * @title: OrgBluezDevice1
 * @short_description: Generated C code for the org.bluez.Device1 D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-org-bluez-Device1.top_of_page">org.bluez.Device1</link> D-Bus interface in C.
 */

/* ---- Introspection data for org.bluez.Device1 ---- */

static const _ExtendedGDBusMethodInfo _org_bluez_device1_method_info_disconnect =
{
  {
    -1,
    (gchar *) "Disconnect",
    NULL,
    NULL,
    NULL
  },
  "handle-disconnect",
  FALSE
};

static const _ExtendedGDBusMethodInfo _org_bluez_device1_method_info_connect =
{
  {
    -1,
    (gchar *) "Connect",
    NULL,
    NULL,
    NULL
  },
  "handle-connect",
  FALSE
};

static const _ExtendedGDBusArgInfo _org_bluez_device1_method_info_connect_profile_IN_ARG_UUID =
{
  {
    -1,
    (gchar *) "UUID",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _org_bluez_device1_method_info_connect_profile_IN_ARG_pointers[] =
{
  &_org_bluez_device1_method_info_connect_profile_IN_ARG_UUID.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _org_bluez_device1_method_info_connect_profile =
{
  {
    -1,
    (gchar *) "ConnectProfile",
    (GDBusArgInfo **) &_org_bluez_device1_method_info_connect_profile_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-connect-profile",
  FALSE
};

static const _ExtendedGDBusArgInfo _org_bluez_device1_method_info_disconnect_profile_IN_ARG_UUID =
{
  {
    -1,
    (gchar *) "UUID",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _org_bluez_device1_method_info_disconnect_profile_IN_ARG_pointers[] =
{
  &_org_bluez_device1_method_info_disconnect_profile_IN_ARG_UUID.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _org_bluez_device1_method_info_disconnect_profile =
{
  {
    -1,
    (gchar *) "DisconnectProfile",
    (GDBusArgInfo **) &_org_bluez_device1_method_info_disconnect_profile_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-disconnect-profile",
  FALSE
};

static const _ExtendedGDBusMethodInfo _org_bluez_device1_method_info_pair =
{
  {
    -1,
    (gchar *) "Pair",
    NULL,
    NULL,
    NULL
  },
  "handle-pair",
  FALSE
};

static const _ExtendedGDBusMethodInfo _org_bluez_device1_method_info_cancel_pairing =
{
  {
    -1,
    (gchar *) "CancelPairing",
    NULL,
    NULL,
    NULL
  },
  "handle-cancel-pairing",
  FALSE
};

static const GDBusMethodInfo * const _org_bluez_device1_method_info_pointers[] =
{
  &_org_bluez_device1_method_info_disconnect.parent_struct,
  &_org_bluez_device1_method_info_connect.parent_struct,
  &_org_bluez_device1_method_info_connect_profile.parent_struct,
  &_org_bluez_device1_method_info_disconnect_profile.parent_struct,
  &_org_bluez_device1_method_info_pair.parent_struct,
  &_org_bluez_device1_method_info_cancel_pairing.parent_struct,
  NULL
};

static const _ExtendedGDBusArgInfo _org_bluez_device1_signal_info_properties_changed_ARG_interface =
{
  {
    -1,
    (gchar *) "interface",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _org_bluez_device1_signal_info_properties_changed_ARG_changed_properties =
{
  {
    -1,
    (gchar *) "changed_properties",
    (gchar *) "a{sv}",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _org_bluez_device1_signal_info_properties_changed_ARG_invalidated_properties =
{
  {
    -1,
    (gchar *) "invalidated_properties",
    (gchar *) "as",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _org_bluez_device1_signal_info_properties_changed_ARG_pointers[] =
{
  &_org_bluez_device1_signal_info_properties_changed_ARG_interface.parent_struct,
  &_org_bluez_device1_signal_info_properties_changed_ARG_changed_properties.parent_struct,
  &_org_bluez_device1_signal_info_properties_changed_ARG_invalidated_properties.parent_struct,
  NULL
};

static const _ExtendedGDBusSignalInfo _org_bluez_device1_signal_info_properties_changed =
{
  {
    -1,
    (gchar *) "PropertiesChanged",
    (GDBusArgInfo **) &_org_bluez_device1_signal_info_properties_changed_ARG_pointers,
    NULL
  },
  "properties-changed"
};

static const GDBusSignalInfo * const _org_bluez_device1_signal_info_pointers[] =
{
  &_org_bluez_device1_signal_info_properties_changed.parent_struct,
  NULL
};

static const _ExtendedGDBusPropertyInfo _org_bluez_device1_property_info_address =
{
  {
    -1,
    (gchar *) "Address",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "address",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _org_bluez_device1_property_info_name =
{
  {
    -1,
    (gchar *) "Name",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "name",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _org_bluez_device1_property_info_alias =
{
  {
    -1,
    (gchar *) "Alias",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE | G_DBUS_PROPERTY_INFO_FLAGS_WRITABLE,
    NULL
  },
  "alias",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _org_bluez_device1_property_info_class =
{
  {
    -1,
    (gchar *) "Class",
    (gchar *) "u",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "class",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _org_bluez_device1_property_info_appearance =
{
  {
    -1,
    (gchar *) "Appearance",
    (gchar *) "q",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "appearance",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _org_bluez_device1_property_info_icon =
{
  {
    -1,
    (gchar *) "Icon",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "icon",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _org_bluez_device1_property_info_paired =
{
  {
    -1,
    (gchar *) "Paired",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "paired",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _org_bluez_device1_property_info_trusted =
{
  {
    -1,
    (gchar *) "Trusted",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE | G_DBUS_PROPERTY_INFO_FLAGS_WRITABLE,
    NULL
  },
  "trusted",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _org_bluez_device1_property_info_blocked =
{
  {
    -1,
    (gchar *) "Blocked",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE | G_DBUS_PROPERTY_INFO_FLAGS_WRITABLE,
    NULL
  },
  "blocked",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _org_bluez_device1_property_info_legacy_pairing =
{
  {
    -1,
    (gchar *) "LegacyPairing",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "legacy-pairing",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _org_bluez_device1_property_info_rssi =
{
  {
    -1,
    (gchar *) "RSSI",
    (gchar *) "n",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "rssi",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _org_bluez_device1_property_info_connected =
{
  {
    -1,
    (gchar *) "Connected",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "connected",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _org_bluez_device1_property_info_uuids =
{
  {
    -1,
    (gchar *) "UUIDs",
    (gchar *) "as",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "uuids",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _org_bluez_device1_property_info_modalias =
{
  {
    -1,
    (gchar *) "Modalias",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "modalias",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _org_bluez_device1_property_info_adapter =
{
  {
    -1,
    (gchar *) "Adapter",
    (gchar *) "o",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "adapter",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _org_bluez_device1_property_info_tx_power =
{
  {
    -1,
    (gchar *) "TxPower",
    (gchar *) "n",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "tx-power",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _org_bluez_device1_property_info_gatt_services =
{
  {
    -1,
    (gchar *) "GattServices",
    (gchar *) "ao",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "gatt-services",
  FALSE,
  TRUE
};

static const GDBusPropertyInfo * const _org_bluez_device1_property_info_pointers[] =
{
  &_org_bluez_device1_property_info_address.parent_struct,
  &_org_bluez_device1_property_info_name.parent_struct,
  &_org_bluez_device1_property_info_alias.parent_struct,
  &_org_bluez_device1_property_info_class.parent_struct,
  &_org_bluez_device1_property_info_appearance.parent_struct,
  &_org_bluez_device1_property_info_icon.parent_struct,
  &_org_bluez_device1_property_info_paired.parent_struct,
  &_org_bluez_device1_property_info_trusted.parent_struct,
  &_org_bluez_device1_property_info_blocked.parent_struct,
  &_org_bluez_device1_property_info_legacy_pairing.parent_struct,
  &_org_bluez_device1_property_info_rssi.parent_struct,
  &_org_bluez_device1_property_info_connected.parent_struct,
  &_org_bluez_device1_property_info_uuids.parent_struct,
  &_org_bluez_device1_property_info_modalias.parent_struct,
  &_org_bluez_device1_property_info_adapter.parent_struct,
  &_org_bluez_device1_property_info_tx_power.parent_struct,
  &_org_bluez_device1_property_info_gatt_services.parent_struct,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _org_bluez_device1_interface_info =
{
  {
    -1,
    (gchar *) "org.bluez.Device1",
    (GDBusMethodInfo **) &_org_bluez_device1_method_info_pointers,
    (GDBusSignalInfo **) &_org_bluez_device1_signal_info_pointers,
    (GDBusPropertyInfo **) &_org_bluez_device1_property_info_pointers,
    NULL
  },
  "org-bluez-device1",
};


/**
 * org_bluez_device1_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-org-bluez-Device1.top_of_page">org.bluez.Device1</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
org_bluez_device1_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_org_bluez_device1_interface_info.parent_struct;
}

/**
 * org_bluez_device1_override_properties:
 * @klass: The class structure for a #GObject derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #OrgBluezDevice1 interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
org_bluez_device1_override_properties (GObjectClass *klass, guint property_id_begin)
{
  g_object_class_override_property (klass, property_id_begin++, "address");
  g_object_class_override_property (klass, property_id_begin++, "name");
  g_object_class_override_property (klass, property_id_begin++, "alias");
  g_object_class_override_property (klass, property_id_begin++, "class");
  g_object_class_override_property (klass, property_id_begin++, "appearance");
  g_object_class_override_property (klass, property_id_begin++, "icon");
  g_object_class_override_property (klass, property_id_begin++, "paired");
  g_object_class_override_property (klass, property_id_begin++, "trusted");
  g_object_class_override_property (klass, property_id_begin++, "blocked");
  g_object_class_override_property (klass, property_id_begin++, "legacy-pairing");
  g_object_class_override_property (klass, property_id_begin++, "rssi");
  g_object_class_override_property (klass, property_id_begin++, "connected");
  g_object_class_override_property (klass, property_id_begin++, "uuids");
  g_object_class_override_property (klass, property_id_begin++, "modalias");
  g_object_class_override_property (klass, property_id_begin++, "adapter");
  g_object_class_override_property (klass, property_id_begin++, "tx-power");
  g_object_class_override_property (klass, property_id_begin++, "gatt-services");
  return property_id_begin - 1;
}



/**
 * OrgBluezDevice1:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-org-bluez-Device1.top_of_page">org.bluez.Device1</link>.
 */

/**
 * OrgBluezDevice1Iface:
 * @parent_iface: The parent interface.
 * @handle_cancel_pairing: Handler for the #OrgBluezDevice1::handle-cancel-pairing signal.
 * @handle_connect: Handler for the #OrgBluezDevice1::handle-connect signal.
 * @handle_connect_profile: Handler for the #OrgBluezDevice1::handle-connect-profile signal.
 * @handle_disconnect: Handler for the #OrgBluezDevice1::handle-disconnect signal.
 * @handle_disconnect_profile: Handler for the #OrgBluezDevice1::handle-disconnect-profile signal.
 * @handle_pair: Handler for the #OrgBluezDevice1::handle-pair signal.
 * @get_adapter: Getter for the #OrgBluezDevice1:adapter property.
 * @get_address: Getter for the #OrgBluezDevice1:address property.
 * @get_alias: Getter for the #OrgBluezDevice1:alias property.
 * @get_appearance: Getter for the #OrgBluezDevice1:appearance property.
 * @get_blocked: Getter for the #OrgBluezDevice1:blocked property.
 * @get_class: Getter for the #OrgBluezDevice1:class property.
 * @get_connected: Getter for the #OrgBluezDevice1:connected property.
 * @get_gatt_services: Getter for the #OrgBluezDevice1:gatt-services property.
 * @get_icon: Getter for the #OrgBluezDevice1:icon property.
 * @get_legacy_pairing: Getter for the #OrgBluezDevice1:legacy-pairing property.
 * @get_modalias: Getter for the #OrgBluezDevice1:modalias property.
 * @get_name: Getter for the #OrgBluezDevice1:name property.
 * @get_paired: Getter for the #OrgBluezDevice1:paired property.
 * @get_rssi: Getter for the #OrgBluezDevice1:rssi property.
 * @get_trusted: Getter for the #OrgBluezDevice1:trusted property.
 * @get_tx_power: Getter for the #OrgBluezDevice1:tx-power property.
 * @get_uuids: Getter for the #OrgBluezDevice1:uuids property.
 * @properties_changed: Handler for the #OrgBluezDevice1::properties-changed signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-org-bluez-Device1.top_of_page">org.bluez.Device1</link>.
 */

typedef OrgBluezDevice1Iface OrgBluezDevice1Interface;
G_DEFINE_INTERFACE (OrgBluezDevice1, org_bluez_device1, G_TYPE_OBJECT)

static void
org_bluez_device1_default_init (OrgBluezDevice1Iface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * OrgBluezDevice1::handle-disconnect:
   * @object: A #OrgBluezDevice1.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-bluez-Device1.Disconnect">Disconnect()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call org_bluez_device1_complete_disconnect() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-disconnect",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (OrgBluezDevice1Iface, handle_disconnect),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * OrgBluezDevice1::handle-connect:
   * @object: A #OrgBluezDevice1.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-bluez-Device1.Connect">Connect()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call org_bluez_device1_complete_connect() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-connect",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (OrgBluezDevice1Iface, handle_connect),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * OrgBluezDevice1::handle-connect-profile:
   * @object: A #OrgBluezDevice1.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_UUID: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-bluez-Device1.ConnectProfile">ConnectProfile()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call org_bluez_device1_complete_connect_profile() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-connect-profile",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (OrgBluezDevice1Iface, handle_connect_profile),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING);

  /**
   * OrgBluezDevice1::handle-disconnect-profile:
   * @object: A #OrgBluezDevice1.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_UUID: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-bluez-Device1.DisconnectProfile">DisconnectProfile()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call org_bluez_device1_complete_disconnect_profile() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-disconnect-profile",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (OrgBluezDevice1Iface, handle_disconnect_profile),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING);

  /**
   * OrgBluezDevice1::handle-pair:
   * @object: A #OrgBluezDevice1.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-bluez-Device1.Pair">Pair()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call org_bluez_device1_complete_pair() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-pair",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (OrgBluezDevice1Iface, handle_pair),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * OrgBluezDevice1::handle-cancel-pairing:
   * @object: A #OrgBluezDevice1.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-bluez-Device1.CancelPairing">CancelPairing()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call org_bluez_device1_complete_cancel_pairing() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-cancel-pairing",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (OrgBluezDevice1Iface, handle_cancel_pairing),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /* GObject signals for received D-Bus signals: */
  /**
   * OrgBluezDevice1::properties-changed:
   * @object: A #OrgBluezDevice1.
   * @arg_interface: Argument.
   * @arg_changed_properties: Argument.
   * @arg_invalidated_properties: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-org-bluez-Device1.PropertiesChanged">"PropertiesChanged"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("properties-changed",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (OrgBluezDevice1Iface, properties_changed),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    3, G_TYPE_STRING, G_TYPE_VARIANT, G_TYPE_STRV);

  /* GObject properties for D-Bus properties: */
  /**
   * OrgBluezDevice1:address:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-Device1.Address">"Address"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("address", "Address", "Address", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * OrgBluezDevice1:name:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-Device1.Name">"Name"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("name", "Name", "Name", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * OrgBluezDevice1:alias:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-Device1.Alias">"Alias"</link>.
   *
   * Since the D-Bus property for this #GObject property is both readable and writable, it is meaningful to both read from it and write to it on both the service- and client-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("alias", "Alias", "Alias", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * OrgBluezDevice1:class:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-Device1.Class">"Class"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_uint ("class", "Class", "Class", 0, G_MAXUINT32, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * OrgBluezDevice1:appearance:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-Device1.Appearance">"Appearance"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_uint ("appearance", "Appearance", "Appearance", 0, G_MAXUINT16, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * OrgBluezDevice1:icon:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-Device1.Icon">"Icon"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("icon", "Icon", "Icon", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * OrgBluezDevice1:paired:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-Device1.Paired">"Paired"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("paired", "Paired", "Paired", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * OrgBluezDevice1:trusted:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-Device1.Trusted">"Trusted"</link>.
   *
   * Since the D-Bus property for this #GObject property is both readable and writable, it is meaningful to both read from it and write to it on both the service- and client-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("trusted", "Trusted", "Trusted", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * OrgBluezDevice1:blocked:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-Device1.Blocked">"Blocked"</link>.
   *
   * Since the D-Bus property for this #GObject property is both readable and writable, it is meaningful to both read from it and write to it on both the service- and client-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("blocked", "Blocked", "Blocked", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * OrgBluezDevice1:legacy-pairing:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-Device1.LegacyPairing">"LegacyPairing"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("legacy-pairing", "LegacyPairing", "LegacyPairing", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * OrgBluezDevice1:rssi:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-Device1.RSSI">"RSSI"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_int ("rssi", "RSSI", "RSSI", G_MININT16, G_MAXINT16, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * OrgBluezDevice1:connected:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-Device1.Connected">"Connected"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("connected", "Connected", "Connected", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * OrgBluezDevice1:uuids:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-Device1.UUIDs">"UUIDs"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boxed ("uuids", "UUIDs", "UUIDs", G_TYPE_STRV, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * OrgBluezDevice1:modalias:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-Device1.Modalias">"Modalias"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("modalias", "Modalias", "Modalias", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * OrgBluezDevice1:adapter:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-Device1.Adapter">"Adapter"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("adapter", "Adapter", "Adapter", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * OrgBluezDevice1:tx-power:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-Device1.TxPower">"TxPower"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_int ("tx-power", "TxPower", "TxPower", G_MININT16, G_MAXINT16, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * OrgBluezDevice1:gatt-services:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-Device1.GattServices">"GattServices"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boxed ("gatt-services", "GattServices", "GattServices", G_TYPE_STRV, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
}

/**
 * org_bluez_device1_get_address: (skip)
 * @object: A #OrgBluezDevice1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-Device1.Address">"Address"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use org_bluez_device1_dup_address() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
org_bluez_device1_get_address (OrgBluezDevice1 *object)
{
  return ORG_BLUEZ_DEVICE1_GET_IFACE (object)->get_address (object);
}

/**
 * org_bluez_device1_dup_address: (skip)
 * @object: A #OrgBluezDevice1.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-bluez-Device1.Address">"Address"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
org_bluez_device1_dup_address (OrgBluezDevice1 *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "address", &value, NULL);
  return value;
}

/**
 * org_bluez_device1_set_address: (skip)
 * @object: A #OrgBluezDevice1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-Device1.Address">"Address"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
org_bluez_device1_set_address (OrgBluezDevice1 *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "address", value, NULL);
}

/**
 * org_bluez_device1_get_name: (skip)
 * @object: A #OrgBluezDevice1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-Device1.Name">"Name"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use org_bluez_device1_dup_name() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
org_bluez_device1_get_name (OrgBluezDevice1 *object)
{
  return ORG_BLUEZ_DEVICE1_GET_IFACE (object)->get_name (object);
}

/**
 * org_bluez_device1_dup_name: (skip)
 * @object: A #OrgBluezDevice1.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-bluez-Device1.Name">"Name"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
org_bluez_device1_dup_name (OrgBluezDevice1 *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "name", &value, NULL);
  return value;
}

/**
 * org_bluez_device1_set_name: (skip)
 * @object: A #OrgBluezDevice1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-Device1.Name">"Name"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
org_bluez_device1_set_name (OrgBluezDevice1 *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "name", value, NULL);
}

/**
 * org_bluez_device1_get_alias: (skip)
 * @object: A #OrgBluezDevice1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-Device1.Alias">"Alias"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use org_bluez_device1_dup_alias() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
org_bluez_device1_get_alias (OrgBluezDevice1 *object)
{
  return ORG_BLUEZ_DEVICE1_GET_IFACE (object)->get_alias (object);
}

/**
 * org_bluez_device1_dup_alias: (skip)
 * @object: A #OrgBluezDevice1.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-bluez-Device1.Alias">"Alias"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
org_bluez_device1_dup_alias (OrgBluezDevice1 *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "alias", &value, NULL);
  return value;
}

/**
 * org_bluez_device1_set_alias: (skip)
 * @object: A #OrgBluezDevice1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-Device1.Alias">"Alias"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 */
void
org_bluez_device1_set_alias (OrgBluezDevice1 *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "alias", value, NULL);
}

/**
 * org_bluez_device1_get_class: (skip)
 * @object: A #OrgBluezDevice1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-Device1.Class">"Class"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
guint 
org_bluez_device1_get_class (OrgBluezDevice1 *object)
{
  return ORG_BLUEZ_DEVICE1_GET_IFACE (object)->get_class (object);
}

/**
 * org_bluez_device1_set_class: (skip)
 * @object: A #OrgBluezDevice1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-Device1.Class">"Class"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
org_bluez_device1_set_class (OrgBluezDevice1 *object, guint value)
{
  g_object_set (G_OBJECT (object), "class", value, NULL);
}

/**
 * org_bluez_device1_get_appearance: (skip)
 * @object: A #OrgBluezDevice1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-Device1.Appearance">"Appearance"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
guint16 
org_bluez_device1_get_appearance (OrgBluezDevice1 *object)
{
  return ORG_BLUEZ_DEVICE1_GET_IFACE (object)->get_appearance (object);
}

/**
 * org_bluez_device1_set_appearance: (skip)
 * @object: A #OrgBluezDevice1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-Device1.Appearance">"Appearance"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
org_bluez_device1_set_appearance (OrgBluezDevice1 *object, guint16 value)
{
  g_object_set (G_OBJECT (object), "appearance", value, NULL);
}

/**
 * org_bluez_device1_get_icon: (skip)
 * @object: A #OrgBluezDevice1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-Device1.Icon">"Icon"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use org_bluez_device1_dup_icon() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
org_bluez_device1_get_icon (OrgBluezDevice1 *object)
{
  return ORG_BLUEZ_DEVICE1_GET_IFACE (object)->get_icon (object);
}

/**
 * org_bluez_device1_dup_icon: (skip)
 * @object: A #OrgBluezDevice1.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-bluez-Device1.Icon">"Icon"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
org_bluez_device1_dup_icon (OrgBluezDevice1 *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "icon", &value, NULL);
  return value;
}

/**
 * org_bluez_device1_set_icon: (skip)
 * @object: A #OrgBluezDevice1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-Device1.Icon">"Icon"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
org_bluez_device1_set_icon (OrgBluezDevice1 *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "icon", value, NULL);
}

/**
 * org_bluez_device1_get_paired: (skip)
 * @object: A #OrgBluezDevice1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-Device1.Paired">"Paired"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
org_bluez_device1_get_paired (OrgBluezDevice1 *object)
{
  return ORG_BLUEZ_DEVICE1_GET_IFACE (object)->get_paired (object);
}

/**
 * org_bluez_device1_set_paired: (skip)
 * @object: A #OrgBluezDevice1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-Device1.Paired">"Paired"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
org_bluez_device1_set_paired (OrgBluezDevice1 *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "paired", value, NULL);
}

/**
 * org_bluez_device1_get_trusted: (skip)
 * @object: A #OrgBluezDevice1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-Device1.Trusted">"Trusted"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
org_bluez_device1_get_trusted (OrgBluezDevice1 *object)
{
  return ORG_BLUEZ_DEVICE1_GET_IFACE (object)->get_trusted (object);
}

/**
 * org_bluez_device1_set_trusted: (skip)
 * @object: A #OrgBluezDevice1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-Device1.Trusted">"Trusted"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 */
void
org_bluez_device1_set_trusted (OrgBluezDevice1 *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "trusted", value, NULL);
}

/**
 * org_bluez_device1_get_blocked: (skip)
 * @object: A #OrgBluezDevice1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-Device1.Blocked">"Blocked"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
org_bluez_device1_get_blocked (OrgBluezDevice1 *object)
{
  return ORG_BLUEZ_DEVICE1_GET_IFACE (object)->get_blocked (object);
}

/**
 * org_bluez_device1_set_blocked: (skip)
 * @object: A #OrgBluezDevice1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-Device1.Blocked">"Blocked"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 */
void
org_bluez_device1_set_blocked (OrgBluezDevice1 *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "blocked", value, NULL);
}

/**
 * org_bluez_device1_get_legacy_pairing: (skip)
 * @object: A #OrgBluezDevice1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-Device1.LegacyPairing">"LegacyPairing"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
org_bluez_device1_get_legacy_pairing (OrgBluezDevice1 *object)
{
  return ORG_BLUEZ_DEVICE1_GET_IFACE (object)->get_legacy_pairing (object);
}

/**
 * org_bluez_device1_set_legacy_pairing: (skip)
 * @object: A #OrgBluezDevice1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-Device1.LegacyPairing">"LegacyPairing"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
org_bluez_device1_set_legacy_pairing (OrgBluezDevice1 *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "legacy-pairing", value, NULL);
}

/**
 * org_bluez_device1_get_rssi: (skip)
 * @object: A #OrgBluezDevice1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-Device1.RSSI">"RSSI"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gint16 
org_bluez_device1_get_rssi (OrgBluezDevice1 *object)
{
  return ORG_BLUEZ_DEVICE1_GET_IFACE (object)->get_rssi (object);
}

/**
 * org_bluez_device1_set_rssi: (skip)
 * @object: A #OrgBluezDevice1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-Device1.RSSI">"RSSI"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
org_bluez_device1_set_rssi (OrgBluezDevice1 *object, gint16 value)
{
  g_object_set (G_OBJECT (object), "rssi", value, NULL);
}

/**
 * org_bluez_device1_get_connected: (skip)
 * @object: A #OrgBluezDevice1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-Device1.Connected">"Connected"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
org_bluez_device1_get_connected (OrgBluezDevice1 *object)
{
  return ORG_BLUEZ_DEVICE1_GET_IFACE (object)->get_connected (object);
}

/**
 * org_bluez_device1_set_connected: (skip)
 * @object: A #OrgBluezDevice1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-Device1.Connected">"Connected"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
org_bluez_device1_set_connected (OrgBluezDevice1 *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "connected", value, NULL);
}

/**
 * org_bluez_device1_get_uuids: (skip)
 * @object: A #OrgBluezDevice1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-Device1.UUIDs">"UUIDs"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use org_bluez_device1_dup_uuids() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *const *
org_bluez_device1_get_uuids (OrgBluezDevice1 *object)
{
  return ORG_BLUEZ_DEVICE1_GET_IFACE (object)->get_uuids (object);
}

/**
 * org_bluez_device1_dup_uuids: (skip)
 * @object: A #OrgBluezDevice1.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-bluez-Device1.UUIDs">"UUIDs"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_strfreev().
 */
gchar **
org_bluez_device1_dup_uuids (OrgBluezDevice1 *object)
{
  gchar **value;
  g_object_get (G_OBJECT (object), "uuids", &value, NULL);
  return value;
}

/**
 * org_bluez_device1_set_uuids: (skip)
 * @object: A #OrgBluezDevice1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-Device1.UUIDs">"UUIDs"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
org_bluez_device1_set_uuids (OrgBluezDevice1 *object, const gchar *const *value)
{
  g_object_set (G_OBJECT (object), "uuids", value, NULL);
}

/**
 * org_bluez_device1_get_modalias: (skip)
 * @object: A #OrgBluezDevice1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-Device1.Modalias">"Modalias"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use org_bluez_device1_dup_modalias() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
org_bluez_device1_get_modalias (OrgBluezDevice1 *object)
{
  return ORG_BLUEZ_DEVICE1_GET_IFACE (object)->get_modalias (object);
}

/**
 * org_bluez_device1_dup_modalias: (skip)
 * @object: A #OrgBluezDevice1.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-bluez-Device1.Modalias">"Modalias"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
org_bluez_device1_dup_modalias (OrgBluezDevice1 *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "modalias", &value, NULL);
  return value;
}

/**
 * org_bluez_device1_set_modalias: (skip)
 * @object: A #OrgBluezDevice1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-Device1.Modalias">"Modalias"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
org_bluez_device1_set_modalias (OrgBluezDevice1 *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "modalias", value, NULL);
}

/**
 * org_bluez_device1_get_adapter: (skip)
 * @object: A #OrgBluezDevice1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-Device1.Adapter">"Adapter"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use org_bluez_device1_dup_adapter() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
org_bluez_device1_get_adapter (OrgBluezDevice1 *object)
{
  return ORG_BLUEZ_DEVICE1_GET_IFACE (object)->get_adapter (object);
}

/**
 * org_bluez_device1_dup_adapter: (skip)
 * @object: A #OrgBluezDevice1.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-bluez-Device1.Adapter">"Adapter"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
org_bluez_device1_dup_adapter (OrgBluezDevice1 *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "adapter", &value, NULL);
  return value;
}

/**
 * org_bluez_device1_set_adapter: (skip)
 * @object: A #OrgBluezDevice1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-Device1.Adapter">"Adapter"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
org_bluez_device1_set_adapter (OrgBluezDevice1 *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "adapter", value, NULL);
}

/**
 * org_bluez_device1_get_tx_power: (skip)
 * @object: A #OrgBluezDevice1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-Device1.TxPower">"TxPower"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gint16 
org_bluez_device1_get_tx_power (OrgBluezDevice1 *object)
{
  return ORG_BLUEZ_DEVICE1_GET_IFACE (object)->get_tx_power (object);
}

/**
 * org_bluez_device1_set_tx_power: (skip)
 * @object: A #OrgBluezDevice1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-Device1.TxPower">"TxPower"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
org_bluez_device1_set_tx_power (OrgBluezDevice1 *object, gint16 value)
{
  g_object_set (G_OBJECT (object), "tx-power", value, NULL);
}

/**
 * org_bluez_device1_get_gatt_services: (skip)
 * @object: A #OrgBluezDevice1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-Device1.GattServices">"GattServices"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use org_bluez_device1_dup_gatt_services() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *const *
org_bluez_device1_get_gatt_services (OrgBluezDevice1 *object)
{
  return ORG_BLUEZ_DEVICE1_GET_IFACE (object)->get_gatt_services (object);
}

/**
 * org_bluez_device1_dup_gatt_services: (skip)
 * @object: A #OrgBluezDevice1.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-bluez-Device1.GattServices">"GattServices"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_strfreev().
 */
gchar **
org_bluez_device1_dup_gatt_services (OrgBluezDevice1 *object)
{
  gchar **value;
  g_object_get (G_OBJECT (object), "gatt-services", &value, NULL);
  return value;
}

/**
 * org_bluez_device1_set_gatt_services: (skip)
 * @object: A #OrgBluezDevice1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-Device1.GattServices">"GattServices"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
org_bluez_device1_set_gatt_services (OrgBluezDevice1 *object, const gchar *const *value)
{
  g_object_set (G_OBJECT (object), "gatt-services", value, NULL);
}

/**
 * org_bluez_device1_emit_properties_changed:
 * @object: A #OrgBluezDevice1.
 * @arg_interface: Argument to pass with the signal.
 * @arg_changed_properties: Argument to pass with the signal.
 * @arg_invalidated_properties: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-org-bluez-Device1.PropertiesChanged">"PropertiesChanged"</link> D-Bus signal.
 */
void
org_bluez_device1_emit_properties_changed (
    OrgBluezDevice1 *object,
    const gchar *arg_interface,
    GVariant *arg_changed_properties,
    const gchar *const *arg_invalidated_properties)
{
  g_signal_emit_by_name (object, "properties-changed", arg_interface, arg_changed_properties, arg_invalidated_properties);
}

/**
 * org_bluez_device1_call_disconnect:
 * @proxy: A #OrgBluezDevice1Proxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-bluez-Device1.Disconnect">Disconnect()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call org_bluez_device1_call_disconnect_finish() to get the result of the operation.
 *
 * See org_bluez_device1_call_disconnect_sync() for the synchronous, blocking version of this method.
 */
void
org_bluez_device1_call_disconnect (
    OrgBluezDevice1 *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Disconnect",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * org_bluez_device1_call_disconnect_finish:
 * @proxy: A #OrgBluezDevice1Proxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to org_bluez_device1_call_disconnect().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with org_bluez_device1_call_disconnect().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
org_bluez_device1_call_disconnect_finish (
    OrgBluezDevice1 *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * org_bluez_device1_call_disconnect_sync:
 * @proxy: A #OrgBluezDevice1Proxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-bluez-Device1.Disconnect">Disconnect()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See org_bluez_device1_call_disconnect() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
org_bluez_device1_call_disconnect_sync (
    OrgBluezDevice1 *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Disconnect",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * org_bluez_device1_call_connect:
 * @proxy: A #OrgBluezDevice1Proxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-bluez-Device1.Connect">Connect()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call org_bluez_device1_call_connect_finish() to get the result of the operation.
 *
 * See org_bluez_device1_call_connect_sync() for the synchronous, blocking version of this method.
 */
void
org_bluez_device1_call_connect (
    OrgBluezDevice1 *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Connect",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * org_bluez_device1_call_connect_finish:
 * @proxy: A #OrgBluezDevice1Proxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to org_bluez_device1_call_connect().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with org_bluez_device1_call_connect().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
org_bluez_device1_call_connect_finish (
    OrgBluezDevice1 *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * org_bluez_device1_call_connect_sync:
 * @proxy: A #OrgBluezDevice1Proxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-bluez-Device1.Connect">Connect()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See org_bluez_device1_call_connect() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
org_bluez_device1_call_connect_sync (
    OrgBluezDevice1 *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Connect",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * org_bluez_device1_call_connect_profile:
 * @proxy: A #OrgBluezDevice1Proxy.
 * @arg_UUID: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-bluez-Device1.ConnectProfile">ConnectProfile()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call org_bluez_device1_call_connect_profile_finish() to get the result of the operation.
 *
 * See org_bluez_device1_call_connect_profile_sync() for the synchronous, blocking version of this method.
 */
void
org_bluez_device1_call_connect_profile (
    OrgBluezDevice1 *proxy,
    const gchar *arg_UUID,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "ConnectProfile",
    g_variant_new ("(s)",
                   arg_UUID),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * org_bluez_device1_call_connect_profile_finish:
 * @proxy: A #OrgBluezDevice1Proxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to org_bluez_device1_call_connect_profile().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with org_bluez_device1_call_connect_profile().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
org_bluez_device1_call_connect_profile_finish (
    OrgBluezDevice1 *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * org_bluez_device1_call_connect_profile_sync:
 * @proxy: A #OrgBluezDevice1Proxy.
 * @arg_UUID: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-bluez-Device1.ConnectProfile">ConnectProfile()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See org_bluez_device1_call_connect_profile() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
org_bluez_device1_call_connect_profile_sync (
    OrgBluezDevice1 *proxy,
    const gchar *arg_UUID,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "ConnectProfile",
    g_variant_new ("(s)",
                   arg_UUID),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * org_bluez_device1_call_disconnect_profile:
 * @proxy: A #OrgBluezDevice1Proxy.
 * @arg_UUID: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-bluez-Device1.DisconnectProfile">DisconnectProfile()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call org_bluez_device1_call_disconnect_profile_finish() to get the result of the operation.
 *
 * See org_bluez_device1_call_disconnect_profile_sync() for the synchronous, blocking version of this method.
 */
void
org_bluez_device1_call_disconnect_profile (
    OrgBluezDevice1 *proxy,
    const gchar *arg_UUID,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "DisconnectProfile",
    g_variant_new ("(s)",
                   arg_UUID),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * org_bluez_device1_call_disconnect_profile_finish:
 * @proxy: A #OrgBluezDevice1Proxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to org_bluez_device1_call_disconnect_profile().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with org_bluez_device1_call_disconnect_profile().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
org_bluez_device1_call_disconnect_profile_finish (
    OrgBluezDevice1 *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * org_bluez_device1_call_disconnect_profile_sync:
 * @proxy: A #OrgBluezDevice1Proxy.
 * @arg_UUID: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-bluez-Device1.DisconnectProfile">DisconnectProfile()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See org_bluez_device1_call_disconnect_profile() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
org_bluez_device1_call_disconnect_profile_sync (
    OrgBluezDevice1 *proxy,
    const gchar *arg_UUID,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "DisconnectProfile",
    g_variant_new ("(s)",
                   arg_UUID),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * org_bluez_device1_call_pair:
 * @proxy: A #OrgBluezDevice1Proxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-bluez-Device1.Pair">Pair()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call org_bluez_device1_call_pair_finish() to get the result of the operation.
 *
 * See org_bluez_device1_call_pair_sync() for the synchronous, blocking version of this method.
 */
void
org_bluez_device1_call_pair (
    OrgBluezDevice1 *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Pair",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * org_bluez_device1_call_pair_finish:
 * @proxy: A #OrgBluezDevice1Proxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to org_bluez_device1_call_pair().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with org_bluez_device1_call_pair().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
org_bluez_device1_call_pair_finish (
    OrgBluezDevice1 *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * org_bluez_device1_call_pair_sync:
 * @proxy: A #OrgBluezDevice1Proxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-bluez-Device1.Pair">Pair()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See org_bluez_device1_call_pair() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
org_bluez_device1_call_pair_sync (
    OrgBluezDevice1 *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Pair",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * org_bluez_device1_call_cancel_pairing:
 * @proxy: A #OrgBluezDevice1Proxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-bluez-Device1.CancelPairing">CancelPairing()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call org_bluez_device1_call_cancel_pairing_finish() to get the result of the operation.
 *
 * See org_bluez_device1_call_cancel_pairing_sync() for the synchronous, blocking version of this method.
 */
void
org_bluez_device1_call_cancel_pairing (
    OrgBluezDevice1 *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "CancelPairing",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * org_bluez_device1_call_cancel_pairing_finish:
 * @proxy: A #OrgBluezDevice1Proxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to org_bluez_device1_call_cancel_pairing().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with org_bluez_device1_call_cancel_pairing().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
org_bluez_device1_call_cancel_pairing_finish (
    OrgBluezDevice1 *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * org_bluez_device1_call_cancel_pairing_sync:
 * @proxy: A #OrgBluezDevice1Proxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-bluez-Device1.CancelPairing">CancelPairing()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See org_bluez_device1_call_cancel_pairing() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
org_bluez_device1_call_cancel_pairing_sync (
    OrgBluezDevice1 *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "CancelPairing",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * org_bluez_device1_complete_disconnect:
 * @object: A #OrgBluezDevice1.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-bluez-Device1.Disconnect">Disconnect()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
org_bluez_device1_complete_disconnect (
    OrgBluezDevice1 *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * org_bluez_device1_complete_connect:
 * @object: A #OrgBluezDevice1.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-bluez-Device1.Connect">Connect()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
org_bluez_device1_complete_connect (
    OrgBluezDevice1 *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * org_bluez_device1_complete_connect_profile:
 * @object: A #OrgBluezDevice1.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-bluez-Device1.ConnectProfile">ConnectProfile()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
org_bluez_device1_complete_connect_profile (
    OrgBluezDevice1 *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * org_bluez_device1_complete_disconnect_profile:
 * @object: A #OrgBluezDevice1.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-bluez-Device1.DisconnectProfile">DisconnectProfile()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
org_bluez_device1_complete_disconnect_profile (
    OrgBluezDevice1 *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * org_bluez_device1_complete_pair:
 * @object: A #OrgBluezDevice1.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-bluez-Device1.Pair">Pair()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
org_bluez_device1_complete_pair (
    OrgBluezDevice1 *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * org_bluez_device1_complete_cancel_pairing:
 * @object: A #OrgBluezDevice1.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-bluez-Device1.CancelPairing">CancelPairing()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
org_bluez_device1_complete_cancel_pairing (
    OrgBluezDevice1 *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/* ------------------------------------------------------------------------ */

/**
 * OrgBluezDevice1Proxy:
 *
 * The #OrgBluezDevice1Proxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * OrgBluezDevice1ProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #OrgBluezDevice1Proxy.
 */

struct _OrgBluezDevice1ProxyPrivate
{
  GData *qdata;
};

static void org_bluez_device1_proxy_iface_init (OrgBluezDevice1Iface *iface);

#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (OrgBluezDevice1Proxy, org_bluez_device1_proxy, G_TYPE_DBUS_PROXY,
                         G_ADD_PRIVATE (OrgBluezDevice1Proxy)
                         G_IMPLEMENT_INTERFACE (TYPE_ORG_BLUEZ_DEVICE1, org_bluez_device1_proxy_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (OrgBluezDevice1Proxy, org_bluez_device1_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (TYPE_ORG_BLUEZ_DEVICE1, org_bluez_device1_proxy_iface_init))

#endif
static void
org_bluez_device1_proxy_finalize (GObject *object)
{
  OrgBluezDevice1Proxy *proxy = ORG_BLUEZ_DEVICE1_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (org_bluez_device1_proxy_parent_class)->finalize (object);
}

static void
org_bluez_device1_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 17);
  info = (const _ExtendedGDBusPropertyInfo *) _org_bluez_device1_property_info_pointers[prop_id - 1];
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (object), info->parent_struct.name);
  if (info->use_gvariant)
    {
      g_value_set_variant (value, variant);
    }
  else
    {
      if (variant != NULL)
        g_dbus_gvariant_to_gvalue (variant, value);
    }
  if (variant != NULL)
    g_variant_unref (variant);
}

static void
org_bluez_device1_proxy_set_property_cb (GDBusProxy *proxy,
  GAsyncResult *res,
  gpointer      user_data)
{
  const _ExtendedGDBusPropertyInfo *info = user_data;
  GError *error;
  GVariant *_ret;
  error = NULL;
  _ret = g_dbus_proxy_call_finish (proxy, res, &error);
  if (!_ret)
    {
      g_warning ("Error setting property '%s' on interface org.bluez.Device1: %s (%s, %d)",
                 info->parent_struct.name, 
                 error->message, g_quark_to_string (error->domain), error->code);
      g_error_free (error);
    }
  else
    {
      g_variant_unref (_ret);
    }
}

static void
org_bluez_device1_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 17);
  info = (const _ExtendedGDBusPropertyInfo *) _org_bluez_device1_property_info_pointers[prop_id - 1];
  variant = g_dbus_gvalue_to_gvariant (value, G_VARIANT_TYPE (info->parent_struct.signature));
  g_dbus_proxy_call (G_DBUS_PROXY (object),
    "org.freedesktop.DBus.Properties.Set",
    g_variant_new ("(ssv)", "org.bluez.Device1", info->parent_struct.name, variant),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    NULL, (GAsyncReadyCallback) org_bluez_device1_proxy_set_property_cb, (GDBusPropertyInfo *) &info->parent_struct);
  g_variant_unref (variant);
}

static void
org_bluez_device1_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name G_GNUC_UNUSED,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  gsize n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_org_bluez_device1_interface_info.parent_struct, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], TYPE_ORG_BLUEZ_DEVICE1);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_ORG_BLUEZ_DEVICE1);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
org_bluez_device1_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  OrgBluezDevice1Proxy *proxy = ORG_BLUEZ_DEVICE1_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_org_bluez_device1_interface_info.parent_struct, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_org_bluez_device1_interface_info.parent_struct, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static const gchar *
org_bluez_device1_proxy_get_address (OrgBluezDevice1 *object)
{
  OrgBluezDevice1Proxy *proxy = ORG_BLUEZ_DEVICE1_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Address");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
org_bluez_device1_proxy_get_name (OrgBluezDevice1 *object)
{
  OrgBluezDevice1Proxy *proxy = ORG_BLUEZ_DEVICE1_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Name");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
org_bluez_device1_proxy_get_alias (OrgBluezDevice1 *object)
{
  OrgBluezDevice1Proxy *proxy = ORG_BLUEZ_DEVICE1_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Alias");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static guint 
org_bluez_device1_proxy_get_class (OrgBluezDevice1 *object)
{
  OrgBluezDevice1Proxy *proxy = ORG_BLUEZ_DEVICE1_PROXY (object);
  GVariant *variant;
  guint value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Class");
  if (variant != NULL)
    {
      value = g_variant_get_uint32 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static guint16 
org_bluez_device1_proxy_get_appearance (OrgBluezDevice1 *object)
{
  OrgBluezDevice1Proxy *proxy = ORG_BLUEZ_DEVICE1_PROXY (object);
  GVariant *variant;
  guint16 value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Appearance");
  if (variant != NULL)
    {
      value = g_variant_get_uint16 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
org_bluez_device1_proxy_get_icon (OrgBluezDevice1 *object)
{
  OrgBluezDevice1Proxy *proxy = ORG_BLUEZ_DEVICE1_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Icon");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean 
org_bluez_device1_proxy_get_paired (OrgBluezDevice1 *object)
{
  OrgBluezDevice1Proxy *proxy = ORG_BLUEZ_DEVICE1_PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Paired");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean 
org_bluez_device1_proxy_get_trusted (OrgBluezDevice1 *object)
{
  OrgBluezDevice1Proxy *proxy = ORG_BLUEZ_DEVICE1_PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Trusted");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean 
org_bluez_device1_proxy_get_blocked (OrgBluezDevice1 *object)
{
  OrgBluezDevice1Proxy *proxy = ORG_BLUEZ_DEVICE1_PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Blocked");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean 
org_bluez_device1_proxy_get_legacy_pairing (OrgBluezDevice1 *object)
{
  OrgBluezDevice1Proxy *proxy = ORG_BLUEZ_DEVICE1_PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "LegacyPairing");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gint16 
org_bluez_device1_proxy_get_rssi (OrgBluezDevice1 *object)
{
  OrgBluezDevice1Proxy *proxy = ORG_BLUEZ_DEVICE1_PROXY (object);
  GVariant *variant;
  gint16 value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "RSSI");
  if (variant != NULL)
    {
      value = g_variant_get_int16 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean 
org_bluez_device1_proxy_get_connected (OrgBluezDevice1 *object)
{
  OrgBluezDevice1Proxy *proxy = ORG_BLUEZ_DEVICE1_PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Connected");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *const *
org_bluez_device1_proxy_get_uuids (OrgBluezDevice1 *object)
{
  OrgBluezDevice1Proxy *proxy = ORG_BLUEZ_DEVICE1_PROXY (object);
  GVariant *variant;
  const gchar *const *value = NULL;
  value = g_datalist_get_data (&proxy->priv->qdata, "UUIDs");
  if (value != NULL)
    return value;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "UUIDs");
  if (variant != NULL)
    {
      value = g_variant_get_strv (variant, NULL);
      g_datalist_set_data_full (&proxy->priv->qdata, "UUIDs", (gpointer) value, g_free);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
org_bluez_device1_proxy_get_modalias (OrgBluezDevice1 *object)
{
  OrgBluezDevice1Proxy *proxy = ORG_BLUEZ_DEVICE1_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Modalias");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
org_bluez_device1_proxy_get_adapter (OrgBluezDevice1 *object)
{
  OrgBluezDevice1Proxy *proxy = ORG_BLUEZ_DEVICE1_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Adapter");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static gint16 
org_bluez_device1_proxy_get_tx_power (OrgBluezDevice1 *object)
{
  OrgBluezDevice1Proxy *proxy = ORG_BLUEZ_DEVICE1_PROXY (object);
  GVariant *variant;
  gint16 value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "TxPower");
  if (variant != NULL)
    {
      value = g_variant_get_int16 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *const *
org_bluez_device1_proxy_get_gatt_services (OrgBluezDevice1 *object)
{
  OrgBluezDevice1Proxy *proxy = ORG_BLUEZ_DEVICE1_PROXY (object);
  GVariant *variant;
  const gchar *const *value = NULL;
  value = g_datalist_get_data (&proxy->priv->qdata, "GattServices");
  if (value != NULL)
    return value;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "GattServices");
  if (variant != NULL)
    {
      value = g_variant_get_objv (variant, NULL);
      g_datalist_set_data_full (&proxy->priv->qdata, "GattServices", (gpointer) value, g_free);
      g_variant_unref (variant);
    }
  return value;
}

static void
org_bluez_device1_proxy_init (OrgBluezDevice1Proxy *proxy)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  proxy->priv = org_bluez_device1_proxy_get_instance_private (proxy);
#else
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, TYPE_ORG_BLUEZ_DEVICE1_PROXY, OrgBluezDevice1ProxyPrivate);
#endif

  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), org_bluez_device1_interface_info ());
}

static void
org_bluez_device1_proxy_class_init (OrgBluezDevice1ProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = org_bluez_device1_proxy_finalize;
  gobject_class->get_property = org_bluez_device1_proxy_get_property;
  gobject_class->set_property = org_bluez_device1_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = org_bluez_device1_proxy_g_signal;
  proxy_class->g_properties_changed = org_bluez_device1_proxy_g_properties_changed;

  org_bluez_device1_override_properties (gobject_class, 1);

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (OrgBluezDevice1ProxyPrivate));
#endif
}

static void
org_bluez_device1_proxy_iface_init (OrgBluezDevice1Iface *iface)
{
  iface->get_address = org_bluez_device1_proxy_get_address;
  iface->get_name = org_bluez_device1_proxy_get_name;
  iface->get_alias = org_bluez_device1_proxy_get_alias;
  iface->get_class = org_bluez_device1_proxy_get_class;
  iface->get_appearance = org_bluez_device1_proxy_get_appearance;
  iface->get_icon = org_bluez_device1_proxy_get_icon;
  iface->get_paired = org_bluez_device1_proxy_get_paired;
  iface->get_trusted = org_bluez_device1_proxy_get_trusted;
  iface->get_blocked = org_bluez_device1_proxy_get_blocked;
  iface->get_legacy_pairing = org_bluez_device1_proxy_get_legacy_pairing;
  iface->get_rssi = org_bluez_device1_proxy_get_rssi;
  iface->get_connected = org_bluez_device1_proxy_get_connected;
  iface->get_uuids = org_bluez_device1_proxy_get_uuids;
  iface->get_modalias = org_bluez_device1_proxy_get_modalias;
  iface->get_adapter = org_bluez_device1_proxy_get_adapter;
  iface->get_tx_power = org_bluez_device1_proxy_get_tx_power;
  iface->get_gatt_services = org_bluez_device1_proxy_get_gatt_services;
}

/**
 * org_bluez_device1_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-bluez-Device1.top_of_page">org.bluez.Device1</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call org_bluez_device1_proxy_new_finish() to get the result of the operation.
 *
 * See org_bluez_device1_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
org_bluez_device1_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_ORG_BLUEZ_DEVICE1_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.bluez.Device1", NULL);
}

/**
 * org_bluez_device1_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to org_bluez_device1_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with org_bluez_device1_proxy_new().
 *
 * Returns: (transfer full) (type OrgBluezDevice1Proxy): The constructed proxy object or %NULL if @error is set.
 */
OrgBluezDevice1 *
org_bluez_device1_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return ORG_BLUEZ_DEVICE1 (ret);
  else
    return NULL;
}

/**
 * org_bluez_device1_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-bluez-Device1.top_of_page">org.bluez.Device1</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See org_bluez_device1_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type OrgBluezDevice1Proxy): The constructed proxy object or %NULL if @error is set.
 */
OrgBluezDevice1 *
org_bluez_device1_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_ORG_BLUEZ_DEVICE1_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.bluez.Device1", NULL);
  if (ret != NULL)
    return ORG_BLUEZ_DEVICE1 (ret);
  else
    return NULL;
}


/**
 * org_bluez_device1_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like org_bluez_device1_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call org_bluez_device1_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See org_bluez_device1_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
org_bluez_device1_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_ORG_BLUEZ_DEVICE1_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.bluez.Device1", NULL);
}

/**
 * org_bluez_device1_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to org_bluez_device1_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with org_bluez_device1_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type OrgBluezDevice1Proxy): The constructed proxy object or %NULL if @error is set.
 */
OrgBluezDevice1 *
org_bluez_device1_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return ORG_BLUEZ_DEVICE1 (ret);
  else
    return NULL;
}

/**
 * org_bluez_device1_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like org_bluez_device1_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See org_bluez_device1_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type OrgBluezDevice1Proxy): The constructed proxy object or %NULL if @error is set.
 */
OrgBluezDevice1 *
org_bluez_device1_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_ORG_BLUEZ_DEVICE1_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.bluez.Device1", NULL);
  if (ret != NULL)
    return ORG_BLUEZ_DEVICE1 (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * OrgBluezDevice1Skeleton:
 *
 * The #OrgBluezDevice1Skeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * OrgBluezDevice1SkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #OrgBluezDevice1Skeleton.
 */

struct _OrgBluezDevice1SkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_org_bluez_device1_skeleton_handle_method_call (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  OrgBluezDevice1Skeleton *skeleton = ORG_BLUEZ_DEVICE1_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  guint num_extra;
  gsize n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], TYPE_ORG_BLUEZ_DEVICE1);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_ORG_BLUEZ_DEVICE1);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_org_bluez_device1_skeleton_handle_get_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  OrgBluezDevice1Skeleton *skeleton = ORG_BLUEZ_DEVICE1_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_org_bluez_device1_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_org_bluez_device1_skeleton_handle_set_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  OrgBluezDevice1Skeleton *skeleton = ORG_BLUEZ_DEVICE1_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_org_bluez_device1_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _org_bluez_device1_skeleton_vtable =
{
  _org_bluez_device1_skeleton_handle_method_call,
  _org_bluez_device1_skeleton_handle_get_property,
  _org_bluez_device1_skeleton_handle_set_property,
  {NULL}
};

static GDBusInterfaceInfo *
org_bluez_device1_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return org_bluez_device1_interface_info ();
}

static GDBusInterfaceVTable *
org_bluez_device1_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return (GDBusInterfaceVTable *) &_org_bluez_device1_skeleton_vtable;
}

static GVariant *
org_bluez_device1_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  OrgBluezDevice1Skeleton *skeleton = ORG_BLUEZ_DEVICE1_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_org_bluez_device1_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _org_bluez_device1_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _org_bluez_device1_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _org_bluez_device1_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.bluez.Device1", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static gboolean _org_bluez_device1_emit_changed (gpointer user_data);

static void
org_bluez_device1_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
  OrgBluezDevice1Skeleton *skeleton = ORG_BLUEZ_DEVICE1_SKELETON (_skeleton);
  gboolean emit_changed = FALSE;

  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    {
      g_source_destroy (skeleton->priv->changed_properties_idle_source);
      skeleton->priv->changed_properties_idle_source = NULL;
      emit_changed = TRUE;
    }
  g_mutex_unlock (&skeleton->priv->lock);

  if (emit_changed)
    _org_bluez_device1_emit_changed (skeleton);
}

static void
_org_bluez_device1_on_signal_properties_changed (
    OrgBluezDevice1 *object,
    const gchar *arg_interface,
    GVariant *arg_changed_properties,
    const gchar *const *arg_invalidated_properties)
{
  OrgBluezDevice1Skeleton *skeleton = ORG_BLUEZ_DEVICE1_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(s@a{sv}^as)",
                   arg_interface,
                   arg_changed_properties,
                   arg_invalidated_properties));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.bluez.Device1", "PropertiesChanged",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void org_bluez_device1_skeleton_iface_init (OrgBluezDevice1Iface *iface);
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (OrgBluezDevice1Skeleton, org_bluez_device1_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_ADD_PRIVATE (OrgBluezDevice1Skeleton)
                         G_IMPLEMENT_INTERFACE (TYPE_ORG_BLUEZ_DEVICE1, org_bluez_device1_skeleton_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (OrgBluezDevice1Skeleton, org_bluez_device1_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (TYPE_ORG_BLUEZ_DEVICE1, org_bluez_device1_skeleton_iface_init))

#endif
static void
org_bluez_device1_skeleton_finalize (GObject *object)
{
  OrgBluezDevice1Skeleton *skeleton = ORG_BLUEZ_DEVICE1_SKELETON (object);
  guint n;
  for (n = 0; n < 17; n++)
    g_value_unset (&skeleton->priv->properties[n]);
  g_free (skeleton->priv->properties);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (org_bluez_device1_skeleton_parent_class)->finalize (object);
}

static void
org_bluez_device1_skeleton_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  OrgBluezDevice1Skeleton *skeleton = ORG_BLUEZ_DEVICE1_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 17);
  g_mutex_lock (&skeleton->priv->lock);
  g_value_copy (&skeleton->priv->properties[prop_id - 1], value);
  g_mutex_unlock (&skeleton->priv->lock);
}

static gboolean
_org_bluez_device1_emit_changed (gpointer user_data)
{
  OrgBluezDevice1Skeleton *skeleton = ORG_BLUEZ_DEVICE1_SKELETON (user_data);
  GList *l;
  GVariantBuilder builder;
  GVariantBuilder invalidated_builder;
  guint num_changes;

  g_mutex_lock (&skeleton->priv->lock);
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  g_variant_builder_init (&invalidated_builder, G_VARIANT_TYPE ("as"));
  for (l = skeleton->priv->changed_properties, num_changes = 0; l != NULL; l = l->next)
    {
      ChangedProperty *cp = l->data;
      GVariant *variant;
      const GValue *cur_value;

      cur_value = &skeleton->priv->properties[cp->prop_id - 1];
      if (!_g_value_equal (cur_value, &cp->orig_value))
        {
          variant = g_dbus_gvalue_to_gvariant (cur_value, G_VARIANT_TYPE (cp->info->parent_struct.signature));
          g_variant_builder_add (&builder, "{sv}", cp->info->parent_struct.name, variant);
          g_variant_unref (variant);
          num_changes++;
        }
    }
  if (num_changes > 0)
    {
      GList *connections, *ll;
      GVariant *signal_variant;
      signal_variant = g_variant_ref_sink (g_variant_new ("(sa{sv}as)", "org.bluez.Device1",
                                           &builder, &invalidated_builder));
      connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));
      for (ll = connections; ll != NULL; ll = ll->next)
        {
          GDBusConnection *connection = ll->data;

          g_dbus_connection_emit_signal (connection,
                                         NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)),
                                         "org.freedesktop.DBus.Properties",
                                         "PropertiesChanged",
                                         signal_variant,
                                         NULL);
        }
      g_variant_unref (signal_variant);
      g_list_free_full (connections, g_object_unref);
    }
  else
    {
      g_variant_builder_clear (&builder);
      g_variant_builder_clear (&invalidated_builder);
    }
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  skeleton->priv->changed_properties = NULL;
  skeleton->priv->changed_properties_idle_source = NULL;
  g_mutex_unlock (&skeleton->priv->lock);
  return FALSE;
}

static void
_org_bluez_device1_schedule_emit_changed (OrgBluezDevice1Skeleton *skeleton, const _ExtendedGDBusPropertyInfo *info, guint prop_id, const GValue *orig_value)
{
  ChangedProperty *cp;
  GList *l;
  cp = NULL;
  for (l = skeleton->priv->changed_properties; l != NULL; l = l->next)
    {
      ChangedProperty *i_cp = l->data;
      if (i_cp->info == info)
        {
          cp = i_cp;
          break;
        }
    }
  if (cp == NULL)
    {
      cp = g_new0 (ChangedProperty, 1);
      cp->prop_id = prop_id;
      cp->info = info;
      skeleton->priv->changed_properties = g_list_prepend (skeleton->priv->changed_properties, cp);
      g_value_init (&cp->orig_value, G_VALUE_TYPE (orig_value));
      g_value_copy (orig_value, &cp->orig_value);
    }
}

static void
org_bluez_device1_skeleton_notify (GObject      *object,
  GParamSpec *pspec G_GNUC_UNUSED)
{
  OrgBluezDevice1Skeleton *skeleton = ORG_BLUEZ_DEVICE1_SKELETON (object);
  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties != NULL &&
      skeleton->priv->changed_properties_idle_source == NULL)
    {
      skeleton->priv->changed_properties_idle_source = g_idle_source_new ();
      g_source_set_priority (skeleton->priv->changed_properties_idle_source, G_PRIORITY_DEFAULT);
      g_source_set_callback (skeleton->priv->changed_properties_idle_source, _org_bluez_device1_emit_changed, g_object_ref (skeleton), (GDestroyNotify) g_object_unref);
      g_source_set_name (skeleton->priv->changed_properties_idle_source, "[generated] _org_bluez_device1_emit_changed");
      g_source_attach (skeleton->priv->changed_properties_idle_source, skeleton->priv->context);
      g_source_unref (skeleton->priv->changed_properties_idle_source);
    }
  g_mutex_unlock (&skeleton->priv->lock);
}

static void
org_bluez_device1_skeleton_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
  const _ExtendedGDBusPropertyInfo *info;
  OrgBluezDevice1Skeleton *skeleton = ORG_BLUEZ_DEVICE1_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 17);
  info = (const _ExtendedGDBusPropertyInfo *) _org_bluez_device1_property_info_pointers[prop_id - 1];
  g_mutex_lock (&skeleton->priv->lock);
  g_object_freeze_notify (object);
  if (!_g_value_equal (value, &skeleton->priv->properties[prop_id - 1]))
    {
      if (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)) != NULL &&
          info->emits_changed_signal)
        _org_bluez_device1_schedule_emit_changed (skeleton, info, prop_id, &skeleton->priv->properties[prop_id - 1]);
      g_value_copy (value, &skeleton->priv->properties[prop_id - 1]);
      g_object_notify_by_pspec (object, pspec);
    }
  g_mutex_unlock (&skeleton->priv->lock);
  g_object_thaw_notify (object);
}

static void
org_bluez_device1_skeleton_init (OrgBluezDevice1Skeleton *skeleton)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  skeleton->priv = org_bluez_device1_skeleton_get_instance_private (skeleton);
#else
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, TYPE_ORG_BLUEZ_DEVICE1_SKELETON, OrgBluezDevice1SkeletonPrivate);
#endif

  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
  skeleton->priv->properties = g_new0 (GValue, 17);
  g_value_init (&skeleton->priv->properties[0], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[1], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[2], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[3], G_TYPE_UINT);
  g_value_init (&skeleton->priv->properties[4], G_TYPE_UINT);
  g_value_init (&skeleton->priv->properties[5], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[6], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[7], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[8], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[9], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[10], G_TYPE_INT);
  g_value_init (&skeleton->priv->properties[11], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[12], G_TYPE_STRV);
  g_value_init (&skeleton->priv->properties[13], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[14], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[15], G_TYPE_INT);
  g_value_init (&skeleton->priv->properties[16], G_TYPE_STRV);
}

static const gchar *
org_bluez_device1_skeleton_get_address (OrgBluezDevice1 *object)
{
  OrgBluezDevice1Skeleton *skeleton = ORG_BLUEZ_DEVICE1_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[0]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
org_bluez_device1_skeleton_get_name (OrgBluezDevice1 *object)
{
  OrgBluezDevice1Skeleton *skeleton = ORG_BLUEZ_DEVICE1_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[1]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
org_bluez_device1_skeleton_get_alias (OrgBluezDevice1 *object)
{
  OrgBluezDevice1Skeleton *skeleton = ORG_BLUEZ_DEVICE1_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[2]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static guint 
org_bluez_device1_skeleton_get_class (OrgBluezDevice1 *object)
{
  OrgBluezDevice1Skeleton *skeleton = ORG_BLUEZ_DEVICE1_SKELETON (object);
  guint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_uint (&(skeleton->priv->properties[3]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static guint16 
org_bluez_device1_skeleton_get_appearance (OrgBluezDevice1 *object)
{
  OrgBluezDevice1Skeleton *skeleton = ORG_BLUEZ_DEVICE1_SKELETON (object);
  guint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_uint (&(skeleton->priv->properties[4]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
org_bluez_device1_skeleton_get_icon (OrgBluezDevice1 *object)
{
  OrgBluezDevice1Skeleton *skeleton = ORG_BLUEZ_DEVICE1_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[5]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean 
org_bluez_device1_skeleton_get_paired (OrgBluezDevice1 *object)
{
  OrgBluezDevice1Skeleton *skeleton = ORG_BLUEZ_DEVICE1_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[6]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean 
org_bluez_device1_skeleton_get_trusted (OrgBluezDevice1 *object)
{
  OrgBluezDevice1Skeleton *skeleton = ORG_BLUEZ_DEVICE1_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[7]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean 
org_bluez_device1_skeleton_get_blocked (OrgBluezDevice1 *object)
{
  OrgBluezDevice1Skeleton *skeleton = ORG_BLUEZ_DEVICE1_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[8]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean 
org_bluez_device1_skeleton_get_legacy_pairing (OrgBluezDevice1 *object)
{
  OrgBluezDevice1Skeleton *skeleton = ORG_BLUEZ_DEVICE1_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[9]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gint16 
org_bluez_device1_skeleton_get_rssi (OrgBluezDevice1 *object)
{
  OrgBluezDevice1Skeleton *skeleton = ORG_BLUEZ_DEVICE1_SKELETON (object);
  gint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_int (&(skeleton->priv->properties[10]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean 
org_bluez_device1_skeleton_get_connected (OrgBluezDevice1 *object)
{
  OrgBluezDevice1Skeleton *skeleton = ORG_BLUEZ_DEVICE1_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[11]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *const *
org_bluez_device1_skeleton_get_uuids (OrgBluezDevice1 *object)
{
  OrgBluezDevice1Skeleton *skeleton = ORG_BLUEZ_DEVICE1_SKELETON (object);
  const gchar *const *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boxed (&(skeleton->priv->properties[12]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
org_bluez_device1_skeleton_get_modalias (OrgBluezDevice1 *object)
{
  OrgBluezDevice1Skeleton *skeleton = ORG_BLUEZ_DEVICE1_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[13]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
org_bluez_device1_skeleton_get_adapter (OrgBluezDevice1 *object)
{
  OrgBluezDevice1Skeleton *skeleton = ORG_BLUEZ_DEVICE1_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[14]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gint16 
org_bluez_device1_skeleton_get_tx_power (OrgBluezDevice1 *object)
{
  OrgBluezDevice1Skeleton *skeleton = ORG_BLUEZ_DEVICE1_SKELETON (object);
  gint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_int (&(skeleton->priv->properties[15]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *const *
org_bluez_device1_skeleton_get_gatt_services (OrgBluezDevice1 *object)
{
  OrgBluezDevice1Skeleton *skeleton = ORG_BLUEZ_DEVICE1_SKELETON (object);
  const gchar *const *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boxed (&(skeleton->priv->properties[16]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static void
org_bluez_device1_skeleton_class_init (OrgBluezDevice1SkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = org_bluez_device1_skeleton_finalize;
  gobject_class->get_property = org_bluez_device1_skeleton_get_property;
  gobject_class->set_property = org_bluez_device1_skeleton_set_property;
  gobject_class->notify       = org_bluez_device1_skeleton_notify;


  org_bluez_device1_override_properties (gobject_class, 1);

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = org_bluez_device1_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = org_bluez_device1_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = org_bluez_device1_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = org_bluez_device1_skeleton_dbus_interface_get_vtable;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (OrgBluezDevice1SkeletonPrivate));
#endif
}

static void
org_bluez_device1_skeleton_iface_init (OrgBluezDevice1Iface *iface)
{
  iface->properties_changed = _org_bluez_device1_on_signal_properties_changed;
  iface->get_address = org_bluez_device1_skeleton_get_address;
  iface->get_name = org_bluez_device1_skeleton_get_name;
  iface->get_alias = org_bluez_device1_skeleton_get_alias;
  iface->get_class = org_bluez_device1_skeleton_get_class;
  iface->get_appearance = org_bluez_device1_skeleton_get_appearance;
  iface->get_icon = org_bluez_device1_skeleton_get_icon;
  iface->get_paired = org_bluez_device1_skeleton_get_paired;
  iface->get_trusted = org_bluez_device1_skeleton_get_trusted;
  iface->get_blocked = org_bluez_device1_skeleton_get_blocked;
  iface->get_legacy_pairing = org_bluez_device1_skeleton_get_legacy_pairing;
  iface->get_rssi = org_bluez_device1_skeleton_get_rssi;
  iface->get_connected = org_bluez_device1_skeleton_get_connected;
  iface->get_uuids = org_bluez_device1_skeleton_get_uuids;
  iface->get_modalias = org_bluez_device1_skeleton_get_modalias;
  iface->get_adapter = org_bluez_device1_skeleton_get_adapter;
  iface->get_tx_power = org_bluez_device1_skeleton_get_tx_power;
  iface->get_gatt_services = org_bluez_device1_skeleton_get_gatt_services;
}

/**
 * org_bluez_device1_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-org-bluez-Device1.top_of_page">org.bluez.Device1</link>.
 *
 * Returns: (transfer full) (type OrgBluezDevice1Skeleton): The skeleton object.
 */
OrgBluezDevice1 *
org_bluez_device1_skeleton_new (void)
{
  return ORG_BLUEZ_DEVICE1 (g_object_new (TYPE_ORG_BLUEZ_DEVICE1_SKELETON, NULL));
}

