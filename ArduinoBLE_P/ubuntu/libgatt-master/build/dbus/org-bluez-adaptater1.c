/*
 * This file is generated by gdbus-codegen, do not modify it.
 *
 * The license of this code is the same as for the D-Bus interface description
 * it was derived from. Note that it links to GLib, so must comply with the
 * LGPL linking clauses.
 */

#ifdef HAVE_CONFIG_H
#  include "config.h"
#endif

#include "/home/paul/Documents/libgatt-master/build/dbus/org-bluez-adaptater1.h"

#include <string.h>
#ifdef G_OS_UNIX
#  include <gio/gunixfdlist.h>
#endif

typedef struct
{
  GDBusArgInfo parent_struct;
  gboolean use_gvariant;
} _ExtendedGDBusArgInfo;

typedef struct
{
  GDBusMethodInfo parent_struct;
  const gchar *signal_name;
  gboolean pass_fdlist;
} _ExtendedGDBusMethodInfo;

typedef struct
{
  GDBusSignalInfo parent_struct;
  const gchar *signal_name;
} _ExtendedGDBusSignalInfo;

typedef struct
{
  GDBusPropertyInfo parent_struct;
  const gchar *hyphen_name;
  guint use_gvariant : 1;
  guint emits_changed_signal : 1;
} _ExtendedGDBusPropertyInfo;

typedef struct
{
  GDBusInterfaceInfo parent_struct;
  const gchar *hyphen_name;
} _ExtendedGDBusInterfaceInfo;

typedef struct
{
  const _ExtendedGDBusPropertyInfo *info;
  guint prop_id;
  GValue orig_value; /* the value before the change */
} ChangedProperty;

static void
_changed_property_free (ChangedProperty *data)
{
  g_value_unset (&data->orig_value);
  g_free (data);
}

static gboolean
_g_strv_equal0 (gchar **a, gchar **b)
{
  gboolean ret = FALSE;
  guint n;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  if (g_strv_length (a) != g_strv_length (b))
    goto out;
  for (n = 0; a[n] != NULL; n++)
    if (g_strcmp0 (a[n], b[n]) != 0)
      goto out;
  ret = TRUE;
out:
  return ret;
}

static gboolean
_g_variant_equal0 (GVariant *a, GVariant *b)
{
  gboolean ret = FALSE;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  ret = g_variant_equal (a, b);
out:
  return ret;
}

G_GNUC_UNUSED static gboolean
_g_value_equal (const GValue *a, const GValue *b)
{
  gboolean ret = FALSE;
  g_assert (G_VALUE_TYPE (a) == G_VALUE_TYPE (b));
  switch (G_VALUE_TYPE (a))
    {
      case G_TYPE_BOOLEAN:
        ret = (g_value_get_boolean (a) == g_value_get_boolean (b));
        break;
      case G_TYPE_UCHAR:
        ret = (g_value_get_uchar (a) == g_value_get_uchar (b));
        break;
      case G_TYPE_INT:
        ret = (g_value_get_int (a) == g_value_get_int (b));
        break;
      case G_TYPE_UINT:
        ret = (g_value_get_uint (a) == g_value_get_uint (b));
        break;
      case G_TYPE_INT64:
        ret = (g_value_get_int64 (a) == g_value_get_int64 (b));
        break;
      case G_TYPE_UINT64:
        ret = (g_value_get_uint64 (a) == g_value_get_uint64 (b));
        break;
      case G_TYPE_DOUBLE:
        {
          /* Avoid -Wfloat-equal warnings by doing a direct bit compare */
          gdouble da = g_value_get_double (a);
          gdouble db = g_value_get_double (b);
          ret = memcmp (&da, &db, sizeof (gdouble)) == 0;
        }
        break;
      case G_TYPE_STRING:
        ret = (g_strcmp0 (g_value_get_string (a), g_value_get_string (b)) == 0);
        break;
      case G_TYPE_VARIANT:
        ret = _g_variant_equal0 (g_value_get_variant (a), g_value_get_variant (b));
        break;
      default:
        if (G_VALUE_TYPE (a) == G_TYPE_STRV)
          ret = _g_strv_equal0 (g_value_get_boxed (a), g_value_get_boxed (b));
        else
          g_critical ("_g_value_equal() does not handle type %s", g_type_name (G_VALUE_TYPE (a)));
        break;
    }
  return ret;
}

/* ------------------------------------------------------------------------
 * Code for interface org.bluez.Adapter1
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:OrgBluezAdapter1
 * @title: OrgBluezAdapter1
 * @short_description: Generated C code for the org.bluez.Adapter1 D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-org-bluez-Adapter1.top_of_page">org.bluez.Adapter1</link> D-Bus interface in C.
 */

/* ---- Introspection data for org.bluez.Adapter1 ---- */

static const _ExtendedGDBusMethodInfo _org_bluez_adapter1_method_info_start_discovery =
{
  {
    -1,
    (gchar *) "StartDiscovery",
    NULL,
    NULL,
    NULL
  },
  "handle-start-discovery",
  FALSE
};

static const _ExtendedGDBusArgInfo _org_bluez_adapter1_method_info_set_discovery_filter_IN_ARG_properties =
{
  {
    -1,
    (gchar *) "properties",
    (gchar *) "a{sv}",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _org_bluez_adapter1_method_info_set_discovery_filter_IN_ARG_pointers[] =
{
  &_org_bluez_adapter1_method_info_set_discovery_filter_IN_ARG_properties.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _org_bluez_adapter1_method_info_set_discovery_filter =
{
  {
    -1,
    (gchar *) "SetDiscoveryFilter",
    (GDBusArgInfo **) &_org_bluez_adapter1_method_info_set_discovery_filter_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-set-discovery-filter",
  FALSE
};

static const _ExtendedGDBusMethodInfo _org_bluez_adapter1_method_info_stop_discovery =
{
  {
    -1,
    (gchar *) "StopDiscovery",
    NULL,
    NULL,
    NULL
  },
  "handle-stop-discovery",
  FALSE
};

static const _ExtendedGDBusArgInfo _org_bluez_adapter1_method_info_remove_device_IN_ARG_device =
{
  {
    -1,
    (gchar *) "device",
    (gchar *) "o",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _org_bluez_adapter1_method_info_remove_device_IN_ARG_pointers[] =
{
  &_org_bluez_adapter1_method_info_remove_device_IN_ARG_device.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _org_bluez_adapter1_method_info_remove_device =
{
  {
    -1,
    (gchar *) "RemoveDevice",
    (GDBusArgInfo **) &_org_bluez_adapter1_method_info_remove_device_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-remove-device",
  FALSE
};

static const GDBusMethodInfo * const _org_bluez_adapter1_method_info_pointers[] =
{
  &_org_bluez_adapter1_method_info_start_discovery.parent_struct,
  &_org_bluez_adapter1_method_info_set_discovery_filter.parent_struct,
  &_org_bluez_adapter1_method_info_stop_discovery.parent_struct,
  &_org_bluez_adapter1_method_info_remove_device.parent_struct,
  NULL
};

static const _ExtendedGDBusPropertyInfo _org_bluez_adapter1_property_info_address =
{
  {
    -1,
    (gchar *) "Address",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "address",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _org_bluez_adapter1_property_info_name =
{
  {
    -1,
    (gchar *) "Name",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "name",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _org_bluez_adapter1_property_info_alias =
{
  {
    -1,
    (gchar *) "Alias",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE | G_DBUS_PROPERTY_INFO_FLAGS_WRITABLE,
    NULL
  },
  "alias",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _org_bluez_adapter1_property_info_class =
{
  {
    -1,
    (gchar *) "Class",
    (gchar *) "u",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "class",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _org_bluez_adapter1_property_info_powered =
{
  {
    -1,
    (gchar *) "Powered",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE | G_DBUS_PROPERTY_INFO_FLAGS_WRITABLE,
    NULL
  },
  "powered",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _org_bluez_adapter1_property_info_discoverable =
{
  {
    -1,
    (gchar *) "Discoverable",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE | G_DBUS_PROPERTY_INFO_FLAGS_WRITABLE,
    NULL
  },
  "discoverable",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _org_bluez_adapter1_property_info_discoverable_timeout =
{
  {
    -1,
    (gchar *) "DiscoverableTimeout",
    (gchar *) "u",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE | G_DBUS_PROPERTY_INFO_FLAGS_WRITABLE,
    NULL
  },
  "discoverable-timeout",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _org_bluez_adapter1_property_info_pairable =
{
  {
    -1,
    (gchar *) "Pairable",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE | G_DBUS_PROPERTY_INFO_FLAGS_WRITABLE,
    NULL
  },
  "pairable",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _org_bluez_adapter1_property_info_pairable_timeout =
{
  {
    -1,
    (gchar *) "PairableTimeout",
    (gchar *) "u",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE | G_DBUS_PROPERTY_INFO_FLAGS_WRITABLE,
    NULL
  },
  "pairable-timeout",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _org_bluez_adapter1_property_info_discovering =
{
  {
    -1,
    (gchar *) "Discovering",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "discovering",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _org_bluez_adapter1_property_info_uuids =
{
  {
    -1,
    (gchar *) "UUIDs",
    (gchar *) "as",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "uuids",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _org_bluez_adapter1_property_info_modalias =
{
  {
    -1,
    (gchar *) "Modalias",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "modalias",
  FALSE,
  TRUE
};

static const GDBusPropertyInfo * const _org_bluez_adapter1_property_info_pointers[] =
{
  &_org_bluez_adapter1_property_info_address.parent_struct,
  &_org_bluez_adapter1_property_info_name.parent_struct,
  &_org_bluez_adapter1_property_info_alias.parent_struct,
  &_org_bluez_adapter1_property_info_class.parent_struct,
  &_org_bluez_adapter1_property_info_powered.parent_struct,
  &_org_bluez_adapter1_property_info_discoverable.parent_struct,
  &_org_bluez_adapter1_property_info_discoverable_timeout.parent_struct,
  &_org_bluez_adapter1_property_info_pairable.parent_struct,
  &_org_bluez_adapter1_property_info_pairable_timeout.parent_struct,
  &_org_bluez_adapter1_property_info_discovering.parent_struct,
  &_org_bluez_adapter1_property_info_uuids.parent_struct,
  &_org_bluez_adapter1_property_info_modalias.parent_struct,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _org_bluez_adapter1_interface_info =
{
  {
    -1,
    (gchar *) "org.bluez.Adapter1",
    (GDBusMethodInfo **) &_org_bluez_adapter1_method_info_pointers,
    NULL,
    (GDBusPropertyInfo **) &_org_bluez_adapter1_property_info_pointers,
    NULL
  },
  "org-bluez-adapter1",
};


/**
 * org_bluez_adapter1_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-org-bluez-Adapter1.top_of_page">org.bluez.Adapter1</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
org_bluez_adapter1_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_org_bluez_adapter1_interface_info.parent_struct;
}

/**
 * org_bluez_adapter1_override_properties:
 * @klass: The class structure for a #GObject derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #OrgBluezAdapter1 interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
org_bluez_adapter1_override_properties (GObjectClass *klass, guint property_id_begin)
{
  g_object_class_override_property (klass, property_id_begin++, "address");
  g_object_class_override_property (klass, property_id_begin++, "name");
  g_object_class_override_property (klass, property_id_begin++, "alias");
  g_object_class_override_property (klass, property_id_begin++, "class");
  g_object_class_override_property (klass, property_id_begin++, "powered");
  g_object_class_override_property (klass, property_id_begin++, "discoverable");
  g_object_class_override_property (klass, property_id_begin++, "discoverable-timeout");
  g_object_class_override_property (klass, property_id_begin++, "pairable");
  g_object_class_override_property (klass, property_id_begin++, "pairable-timeout");
  g_object_class_override_property (klass, property_id_begin++, "discovering");
  g_object_class_override_property (klass, property_id_begin++, "uuids");
  g_object_class_override_property (klass, property_id_begin++, "modalias");
  return property_id_begin - 1;
}



/**
 * OrgBluezAdapter1:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-org-bluez-Adapter1.top_of_page">org.bluez.Adapter1</link>.
 */

/**
 * OrgBluezAdapter1Iface:
 * @parent_iface: The parent interface.
 * @handle_remove_device: Handler for the #OrgBluezAdapter1::handle-remove-device signal.
 * @handle_set_discovery_filter: Handler for the #OrgBluezAdapter1::handle-set-discovery-filter signal.
 * @handle_start_discovery: Handler for the #OrgBluezAdapter1::handle-start-discovery signal.
 * @handle_stop_discovery: Handler for the #OrgBluezAdapter1::handle-stop-discovery signal.
 * @get_address: Getter for the #OrgBluezAdapter1:address property.
 * @get_alias: Getter for the #OrgBluezAdapter1:alias property.
 * @get_class: Getter for the #OrgBluezAdapter1:class property.
 * @get_discoverable: Getter for the #OrgBluezAdapter1:discoverable property.
 * @get_discoverable_timeout: Getter for the #OrgBluezAdapter1:discoverable-timeout property.
 * @get_discovering: Getter for the #OrgBluezAdapter1:discovering property.
 * @get_modalias: Getter for the #OrgBluezAdapter1:modalias property.
 * @get_name: Getter for the #OrgBluezAdapter1:name property.
 * @get_pairable: Getter for the #OrgBluezAdapter1:pairable property.
 * @get_pairable_timeout: Getter for the #OrgBluezAdapter1:pairable-timeout property.
 * @get_powered: Getter for the #OrgBluezAdapter1:powered property.
 * @get_uuids: Getter for the #OrgBluezAdapter1:uuids property.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-org-bluez-Adapter1.top_of_page">org.bluez.Adapter1</link>.
 */

typedef OrgBluezAdapter1Iface OrgBluezAdapter1Interface;
G_DEFINE_INTERFACE (OrgBluezAdapter1, org_bluez_adapter1, G_TYPE_OBJECT)

static void
org_bluez_adapter1_default_init (OrgBluezAdapter1Iface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * OrgBluezAdapter1::handle-start-discovery:
   * @object: A #OrgBluezAdapter1.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-bluez-Adapter1.StartDiscovery">StartDiscovery()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call org_bluez_adapter1_complete_start_discovery() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-start-discovery",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (OrgBluezAdapter1Iface, handle_start_discovery),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * OrgBluezAdapter1::handle-set-discovery-filter:
   * @object: A #OrgBluezAdapter1.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_properties: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-bluez-Adapter1.SetDiscoveryFilter">SetDiscoveryFilter()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call org_bluez_adapter1_complete_set_discovery_filter() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-set-discovery-filter",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (OrgBluezAdapter1Iface, handle_set_discovery_filter),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_VARIANT);

  /**
   * OrgBluezAdapter1::handle-stop-discovery:
   * @object: A #OrgBluezAdapter1.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-bluez-Adapter1.StopDiscovery">StopDiscovery()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call org_bluez_adapter1_complete_stop_discovery() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-stop-discovery",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (OrgBluezAdapter1Iface, handle_stop_discovery),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * OrgBluezAdapter1::handle-remove-device:
   * @object: A #OrgBluezAdapter1.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_device: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-bluez-Adapter1.RemoveDevice">RemoveDevice()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call org_bluez_adapter1_complete_remove_device() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-remove-device",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (OrgBluezAdapter1Iface, handle_remove_device),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING);

  /* GObject properties for D-Bus properties: */
  /**
   * OrgBluezAdapter1:address:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-Adapter1.Address">"Address"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("address", "Address", "Address", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * OrgBluezAdapter1:name:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-Adapter1.Name">"Name"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("name", "Name", "Name", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * OrgBluezAdapter1:alias:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-Adapter1.Alias">"Alias"</link>.
   *
   * Since the D-Bus property for this #GObject property is both readable and writable, it is meaningful to both read from it and write to it on both the service- and client-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("alias", "Alias", "Alias", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * OrgBluezAdapter1:class:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-Adapter1.Class">"Class"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_uint ("class", "Class", "Class", 0, G_MAXUINT32, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * OrgBluezAdapter1:powered:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-Adapter1.Powered">"Powered"</link>.
   *
   * Since the D-Bus property for this #GObject property is both readable and writable, it is meaningful to both read from it and write to it on both the service- and client-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("powered", "Powered", "Powered", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * OrgBluezAdapter1:discoverable:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-Adapter1.Discoverable">"Discoverable"</link>.
   *
   * Since the D-Bus property for this #GObject property is both readable and writable, it is meaningful to both read from it and write to it on both the service- and client-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("discoverable", "Discoverable", "Discoverable", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * OrgBluezAdapter1:discoverable-timeout:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-Adapter1.DiscoverableTimeout">"DiscoverableTimeout"</link>.
   *
   * Since the D-Bus property for this #GObject property is both readable and writable, it is meaningful to both read from it and write to it on both the service- and client-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_uint ("discoverable-timeout", "DiscoverableTimeout", "DiscoverableTimeout", 0, G_MAXUINT32, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * OrgBluezAdapter1:pairable:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-Adapter1.Pairable">"Pairable"</link>.
   *
   * Since the D-Bus property for this #GObject property is both readable and writable, it is meaningful to both read from it and write to it on both the service- and client-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("pairable", "Pairable", "Pairable", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * OrgBluezAdapter1:pairable-timeout:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-Adapter1.PairableTimeout">"PairableTimeout"</link>.
   *
   * Since the D-Bus property for this #GObject property is both readable and writable, it is meaningful to both read from it and write to it on both the service- and client-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_uint ("pairable-timeout", "PairableTimeout", "PairableTimeout", 0, G_MAXUINT32, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * OrgBluezAdapter1:discovering:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-Adapter1.Discovering">"Discovering"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("discovering", "Discovering", "Discovering", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * OrgBluezAdapter1:uuids:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-Adapter1.UUIDs">"UUIDs"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boxed ("uuids", "UUIDs", "UUIDs", G_TYPE_STRV, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * OrgBluezAdapter1:modalias:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-Adapter1.Modalias">"Modalias"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("modalias", "Modalias", "Modalias", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
}

/**
 * org_bluez_adapter1_get_address: (skip)
 * @object: A #OrgBluezAdapter1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-Adapter1.Address">"Address"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use org_bluez_adapter1_dup_address() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
org_bluez_adapter1_get_address (OrgBluezAdapter1 *object)
{
  return ORG_BLUEZ_ADAPTER1_GET_IFACE (object)->get_address (object);
}

/**
 * org_bluez_adapter1_dup_address: (skip)
 * @object: A #OrgBluezAdapter1.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-bluez-Adapter1.Address">"Address"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
org_bluez_adapter1_dup_address (OrgBluezAdapter1 *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "address", &value, NULL);
  return value;
}

/**
 * org_bluez_adapter1_set_address: (skip)
 * @object: A #OrgBluezAdapter1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-Adapter1.Address">"Address"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
org_bluez_adapter1_set_address (OrgBluezAdapter1 *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "address", value, NULL);
}

/**
 * org_bluez_adapter1_get_name: (skip)
 * @object: A #OrgBluezAdapter1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-Adapter1.Name">"Name"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use org_bluez_adapter1_dup_name() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
org_bluez_adapter1_get_name (OrgBluezAdapter1 *object)
{
  return ORG_BLUEZ_ADAPTER1_GET_IFACE (object)->get_name (object);
}

/**
 * org_bluez_adapter1_dup_name: (skip)
 * @object: A #OrgBluezAdapter1.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-bluez-Adapter1.Name">"Name"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
org_bluez_adapter1_dup_name (OrgBluezAdapter1 *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "name", &value, NULL);
  return value;
}

/**
 * org_bluez_adapter1_set_name: (skip)
 * @object: A #OrgBluezAdapter1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-Adapter1.Name">"Name"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
org_bluez_adapter1_set_name (OrgBluezAdapter1 *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "name", value, NULL);
}

/**
 * org_bluez_adapter1_get_alias: (skip)
 * @object: A #OrgBluezAdapter1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-Adapter1.Alias">"Alias"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use org_bluez_adapter1_dup_alias() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
org_bluez_adapter1_get_alias (OrgBluezAdapter1 *object)
{
  return ORG_BLUEZ_ADAPTER1_GET_IFACE (object)->get_alias (object);
}

/**
 * org_bluez_adapter1_dup_alias: (skip)
 * @object: A #OrgBluezAdapter1.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-bluez-Adapter1.Alias">"Alias"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
org_bluez_adapter1_dup_alias (OrgBluezAdapter1 *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "alias", &value, NULL);
  return value;
}

/**
 * org_bluez_adapter1_set_alias: (skip)
 * @object: A #OrgBluezAdapter1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-Adapter1.Alias">"Alias"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 */
void
org_bluez_adapter1_set_alias (OrgBluezAdapter1 *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "alias", value, NULL);
}

/**
 * org_bluez_adapter1_get_class: (skip)
 * @object: A #OrgBluezAdapter1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-Adapter1.Class">"Class"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
guint 
org_bluez_adapter1_get_class (OrgBluezAdapter1 *object)
{
  return ORG_BLUEZ_ADAPTER1_GET_IFACE (object)->get_class (object);
}

/**
 * org_bluez_adapter1_set_class: (skip)
 * @object: A #OrgBluezAdapter1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-Adapter1.Class">"Class"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
org_bluez_adapter1_set_class (OrgBluezAdapter1 *object, guint value)
{
  g_object_set (G_OBJECT (object), "class", value, NULL);
}

/**
 * org_bluez_adapter1_get_powered: (skip)
 * @object: A #OrgBluezAdapter1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-Adapter1.Powered">"Powered"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
org_bluez_adapter1_get_powered (OrgBluezAdapter1 *object)
{
  return ORG_BLUEZ_ADAPTER1_GET_IFACE (object)->get_powered (object);
}

/**
 * org_bluez_adapter1_set_powered: (skip)
 * @object: A #OrgBluezAdapter1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-Adapter1.Powered">"Powered"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 */
void
org_bluez_adapter1_set_powered (OrgBluezAdapter1 *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "powered", value, NULL);
}

/**
 * org_bluez_adapter1_get_discoverable: (skip)
 * @object: A #OrgBluezAdapter1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-Adapter1.Discoverable">"Discoverable"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
org_bluez_adapter1_get_discoverable (OrgBluezAdapter1 *object)
{
  return ORG_BLUEZ_ADAPTER1_GET_IFACE (object)->get_discoverable (object);
}

/**
 * org_bluez_adapter1_set_discoverable: (skip)
 * @object: A #OrgBluezAdapter1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-Adapter1.Discoverable">"Discoverable"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 */
void
org_bluez_adapter1_set_discoverable (OrgBluezAdapter1 *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "discoverable", value, NULL);
}

/**
 * org_bluez_adapter1_get_discoverable_timeout: (skip)
 * @object: A #OrgBluezAdapter1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-Adapter1.DiscoverableTimeout">"DiscoverableTimeout"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
guint 
org_bluez_adapter1_get_discoverable_timeout (OrgBluezAdapter1 *object)
{
  return ORG_BLUEZ_ADAPTER1_GET_IFACE (object)->get_discoverable_timeout (object);
}

/**
 * org_bluez_adapter1_set_discoverable_timeout: (skip)
 * @object: A #OrgBluezAdapter1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-Adapter1.DiscoverableTimeout">"DiscoverableTimeout"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 */
void
org_bluez_adapter1_set_discoverable_timeout (OrgBluezAdapter1 *object, guint value)
{
  g_object_set (G_OBJECT (object), "discoverable-timeout", value, NULL);
}

/**
 * org_bluez_adapter1_get_pairable: (skip)
 * @object: A #OrgBluezAdapter1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-Adapter1.Pairable">"Pairable"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
org_bluez_adapter1_get_pairable (OrgBluezAdapter1 *object)
{
  return ORG_BLUEZ_ADAPTER1_GET_IFACE (object)->get_pairable (object);
}

/**
 * org_bluez_adapter1_set_pairable: (skip)
 * @object: A #OrgBluezAdapter1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-Adapter1.Pairable">"Pairable"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 */
void
org_bluez_adapter1_set_pairable (OrgBluezAdapter1 *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "pairable", value, NULL);
}

/**
 * org_bluez_adapter1_get_pairable_timeout: (skip)
 * @object: A #OrgBluezAdapter1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-Adapter1.PairableTimeout">"PairableTimeout"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
guint 
org_bluez_adapter1_get_pairable_timeout (OrgBluezAdapter1 *object)
{
  return ORG_BLUEZ_ADAPTER1_GET_IFACE (object)->get_pairable_timeout (object);
}

/**
 * org_bluez_adapter1_set_pairable_timeout: (skip)
 * @object: A #OrgBluezAdapter1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-Adapter1.PairableTimeout">"PairableTimeout"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 */
void
org_bluez_adapter1_set_pairable_timeout (OrgBluezAdapter1 *object, guint value)
{
  g_object_set (G_OBJECT (object), "pairable-timeout", value, NULL);
}

/**
 * org_bluez_adapter1_get_discovering: (skip)
 * @object: A #OrgBluezAdapter1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-Adapter1.Discovering">"Discovering"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
org_bluez_adapter1_get_discovering (OrgBluezAdapter1 *object)
{
  return ORG_BLUEZ_ADAPTER1_GET_IFACE (object)->get_discovering (object);
}

/**
 * org_bluez_adapter1_set_discovering: (skip)
 * @object: A #OrgBluezAdapter1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-Adapter1.Discovering">"Discovering"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
org_bluez_adapter1_set_discovering (OrgBluezAdapter1 *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "discovering", value, NULL);
}

/**
 * org_bluez_adapter1_get_uuids: (skip)
 * @object: A #OrgBluezAdapter1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-Adapter1.UUIDs">"UUIDs"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use org_bluez_adapter1_dup_uuids() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *const *
org_bluez_adapter1_get_uuids (OrgBluezAdapter1 *object)
{
  return ORG_BLUEZ_ADAPTER1_GET_IFACE (object)->get_uuids (object);
}

/**
 * org_bluez_adapter1_dup_uuids: (skip)
 * @object: A #OrgBluezAdapter1.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-bluez-Adapter1.UUIDs">"UUIDs"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_strfreev().
 */
gchar **
org_bluez_adapter1_dup_uuids (OrgBluezAdapter1 *object)
{
  gchar **value;
  g_object_get (G_OBJECT (object), "uuids", &value, NULL);
  return value;
}

/**
 * org_bluez_adapter1_set_uuids: (skip)
 * @object: A #OrgBluezAdapter1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-Adapter1.UUIDs">"UUIDs"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
org_bluez_adapter1_set_uuids (OrgBluezAdapter1 *object, const gchar *const *value)
{
  g_object_set (G_OBJECT (object), "uuids", value, NULL);
}

/**
 * org_bluez_adapter1_get_modalias: (skip)
 * @object: A #OrgBluezAdapter1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-Adapter1.Modalias">"Modalias"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use org_bluez_adapter1_dup_modalias() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
org_bluez_adapter1_get_modalias (OrgBluezAdapter1 *object)
{
  return ORG_BLUEZ_ADAPTER1_GET_IFACE (object)->get_modalias (object);
}

/**
 * org_bluez_adapter1_dup_modalias: (skip)
 * @object: A #OrgBluezAdapter1.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-bluez-Adapter1.Modalias">"Modalias"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
org_bluez_adapter1_dup_modalias (OrgBluezAdapter1 *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "modalias", &value, NULL);
  return value;
}

/**
 * org_bluez_adapter1_set_modalias: (skip)
 * @object: A #OrgBluezAdapter1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-Adapter1.Modalias">"Modalias"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
org_bluez_adapter1_set_modalias (OrgBluezAdapter1 *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "modalias", value, NULL);
}

/**
 * org_bluez_adapter1_call_start_discovery:
 * @proxy: A #OrgBluezAdapter1Proxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-bluez-Adapter1.StartDiscovery">StartDiscovery()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call org_bluez_adapter1_call_start_discovery_finish() to get the result of the operation.
 *
 * See org_bluez_adapter1_call_start_discovery_sync() for the synchronous, blocking version of this method.
 */
void
org_bluez_adapter1_call_start_discovery (
    OrgBluezAdapter1 *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "StartDiscovery",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * org_bluez_adapter1_call_start_discovery_finish:
 * @proxy: A #OrgBluezAdapter1Proxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to org_bluez_adapter1_call_start_discovery().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with org_bluez_adapter1_call_start_discovery().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
org_bluez_adapter1_call_start_discovery_finish (
    OrgBluezAdapter1 *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * org_bluez_adapter1_call_start_discovery_sync:
 * @proxy: A #OrgBluezAdapter1Proxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-bluez-Adapter1.StartDiscovery">StartDiscovery()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See org_bluez_adapter1_call_start_discovery() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
org_bluez_adapter1_call_start_discovery_sync (
    OrgBluezAdapter1 *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "StartDiscovery",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * org_bluez_adapter1_call_set_discovery_filter:
 * @proxy: A #OrgBluezAdapter1Proxy.
 * @arg_properties: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-bluez-Adapter1.SetDiscoveryFilter">SetDiscoveryFilter()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call org_bluez_adapter1_call_set_discovery_filter_finish() to get the result of the operation.
 *
 * See org_bluez_adapter1_call_set_discovery_filter_sync() for the synchronous, blocking version of this method.
 */
void
org_bluez_adapter1_call_set_discovery_filter (
    OrgBluezAdapter1 *proxy,
    GVariant *arg_properties,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "SetDiscoveryFilter",
    g_variant_new ("(@a{sv})",
                   arg_properties),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * org_bluez_adapter1_call_set_discovery_filter_finish:
 * @proxy: A #OrgBluezAdapter1Proxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to org_bluez_adapter1_call_set_discovery_filter().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with org_bluez_adapter1_call_set_discovery_filter().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
org_bluez_adapter1_call_set_discovery_filter_finish (
    OrgBluezAdapter1 *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * org_bluez_adapter1_call_set_discovery_filter_sync:
 * @proxy: A #OrgBluezAdapter1Proxy.
 * @arg_properties: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-bluez-Adapter1.SetDiscoveryFilter">SetDiscoveryFilter()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See org_bluez_adapter1_call_set_discovery_filter() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
org_bluez_adapter1_call_set_discovery_filter_sync (
    OrgBluezAdapter1 *proxy,
    GVariant *arg_properties,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "SetDiscoveryFilter",
    g_variant_new ("(@a{sv})",
                   arg_properties),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * org_bluez_adapter1_call_stop_discovery:
 * @proxy: A #OrgBluezAdapter1Proxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-bluez-Adapter1.StopDiscovery">StopDiscovery()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call org_bluez_adapter1_call_stop_discovery_finish() to get the result of the operation.
 *
 * See org_bluez_adapter1_call_stop_discovery_sync() for the synchronous, blocking version of this method.
 */
void
org_bluez_adapter1_call_stop_discovery (
    OrgBluezAdapter1 *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "StopDiscovery",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * org_bluez_adapter1_call_stop_discovery_finish:
 * @proxy: A #OrgBluezAdapter1Proxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to org_bluez_adapter1_call_stop_discovery().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with org_bluez_adapter1_call_stop_discovery().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
org_bluez_adapter1_call_stop_discovery_finish (
    OrgBluezAdapter1 *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * org_bluez_adapter1_call_stop_discovery_sync:
 * @proxy: A #OrgBluezAdapter1Proxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-bluez-Adapter1.StopDiscovery">StopDiscovery()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See org_bluez_adapter1_call_stop_discovery() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
org_bluez_adapter1_call_stop_discovery_sync (
    OrgBluezAdapter1 *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "StopDiscovery",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * org_bluez_adapter1_call_remove_device:
 * @proxy: A #OrgBluezAdapter1Proxy.
 * @arg_device: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-bluez-Adapter1.RemoveDevice">RemoveDevice()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call org_bluez_adapter1_call_remove_device_finish() to get the result of the operation.
 *
 * See org_bluez_adapter1_call_remove_device_sync() for the synchronous, blocking version of this method.
 */
void
org_bluez_adapter1_call_remove_device (
    OrgBluezAdapter1 *proxy,
    const gchar *arg_device,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "RemoveDevice",
    g_variant_new ("(o)",
                   arg_device),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * org_bluez_adapter1_call_remove_device_finish:
 * @proxy: A #OrgBluezAdapter1Proxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to org_bluez_adapter1_call_remove_device().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with org_bluez_adapter1_call_remove_device().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
org_bluez_adapter1_call_remove_device_finish (
    OrgBluezAdapter1 *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * org_bluez_adapter1_call_remove_device_sync:
 * @proxy: A #OrgBluezAdapter1Proxy.
 * @arg_device: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-bluez-Adapter1.RemoveDevice">RemoveDevice()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See org_bluez_adapter1_call_remove_device() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
org_bluez_adapter1_call_remove_device_sync (
    OrgBluezAdapter1 *proxy,
    const gchar *arg_device,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "RemoveDevice",
    g_variant_new ("(o)",
                   arg_device),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * org_bluez_adapter1_complete_start_discovery:
 * @object: A #OrgBluezAdapter1.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-bluez-Adapter1.StartDiscovery">StartDiscovery()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
org_bluez_adapter1_complete_start_discovery (
    OrgBluezAdapter1 *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * org_bluez_adapter1_complete_set_discovery_filter:
 * @object: A #OrgBluezAdapter1.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-bluez-Adapter1.SetDiscoveryFilter">SetDiscoveryFilter()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
org_bluez_adapter1_complete_set_discovery_filter (
    OrgBluezAdapter1 *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * org_bluez_adapter1_complete_stop_discovery:
 * @object: A #OrgBluezAdapter1.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-bluez-Adapter1.StopDiscovery">StopDiscovery()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
org_bluez_adapter1_complete_stop_discovery (
    OrgBluezAdapter1 *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * org_bluez_adapter1_complete_remove_device:
 * @object: A #OrgBluezAdapter1.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-bluez-Adapter1.RemoveDevice">RemoveDevice()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
org_bluez_adapter1_complete_remove_device (
    OrgBluezAdapter1 *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/* ------------------------------------------------------------------------ */

/**
 * OrgBluezAdapter1Proxy:
 *
 * The #OrgBluezAdapter1Proxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * OrgBluezAdapter1ProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #OrgBluezAdapter1Proxy.
 */

struct _OrgBluezAdapter1ProxyPrivate
{
  GData *qdata;
};

static void org_bluez_adapter1_proxy_iface_init (OrgBluezAdapter1Iface *iface);

#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (OrgBluezAdapter1Proxy, org_bluez_adapter1_proxy, G_TYPE_DBUS_PROXY,
                         G_ADD_PRIVATE (OrgBluezAdapter1Proxy)
                         G_IMPLEMENT_INTERFACE (TYPE_ORG_BLUEZ_ADAPTER1, org_bluez_adapter1_proxy_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (OrgBluezAdapter1Proxy, org_bluez_adapter1_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (TYPE_ORG_BLUEZ_ADAPTER1, org_bluez_adapter1_proxy_iface_init))

#endif
static void
org_bluez_adapter1_proxy_finalize (GObject *object)
{
  OrgBluezAdapter1Proxy *proxy = ORG_BLUEZ_ADAPTER1_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (org_bluez_adapter1_proxy_parent_class)->finalize (object);
}

static void
org_bluez_adapter1_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 12);
  info = (const _ExtendedGDBusPropertyInfo *) _org_bluez_adapter1_property_info_pointers[prop_id - 1];
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (object), info->parent_struct.name);
  if (info->use_gvariant)
    {
      g_value_set_variant (value, variant);
    }
  else
    {
      if (variant != NULL)
        g_dbus_gvariant_to_gvalue (variant, value);
    }
  if (variant != NULL)
    g_variant_unref (variant);
}

static void
org_bluez_adapter1_proxy_set_property_cb (GDBusProxy *proxy,
  GAsyncResult *res,
  gpointer      user_data)
{
  const _ExtendedGDBusPropertyInfo *info = user_data;
  GError *error;
  GVariant *_ret;
  error = NULL;
  _ret = g_dbus_proxy_call_finish (proxy, res, &error);
  if (!_ret)
    {
      g_warning ("Error setting property '%s' on interface org.bluez.Adapter1: %s (%s, %d)",
                 info->parent_struct.name, 
                 error->message, g_quark_to_string (error->domain), error->code);
      g_error_free (error);
    }
  else
    {
      g_variant_unref (_ret);
    }
}

static void
org_bluez_adapter1_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 12);
  info = (const _ExtendedGDBusPropertyInfo *) _org_bluez_adapter1_property_info_pointers[prop_id - 1];
  variant = g_dbus_gvalue_to_gvariant (value, G_VARIANT_TYPE (info->parent_struct.signature));
  g_dbus_proxy_call (G_DBUS_PROXY (object),
    "org.freedesktop.DBus.Properties.Set",
    g_variant_new ("(ssv)", "org.bluez.Adapter1", info->parent_struct.name, variant),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    NULL, (GAsyncReadyCallback) org_bluez_adapter1_proxy_set_property_cb, (GDBusPropertyInfo *) &info->parent_struct);
  g_variant_unref (variant);
}

static void
org_bluez_adapter1_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name G_GNUC_UNUSED,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  gsize n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_org_bluez_adapter1_interface_info.parent_struct, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], TYPE_ORG_BLUEZ_ADAPTER1);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_ORG_BLUEZ_ADAPTER1);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
org_bluez_adapter1_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  OrgBluezAdapter1Proxy *proxy = ORG_BLUEZ_ADAPTER1_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_org_bluez_adapter1_interface_info.parent_struct, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_org_bluez_adapter1_interface_info.parent_struct, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static const gchar *
org_bluez_adapter1_proxy_get_address (OrgBluezAdapter1 *object)
{
  OrgBluezAdapter1Proxy *proxy = ORG_BLUEZ_ADAPTER1_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Address");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
org_bluez_adapter1_proxy_get_name (OrgBluezAdapter1 *object)
{
  OrgBluezAdapter1Proxy *proxy = ORG_BLUEZ_ADAPTER1_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Name");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
org_bluez_adapter1_proxy_get_alias (OrgBluezAdapter1 *object)
{
  OrgBluezAdapter1Proxy *proxy = ORG_BLUEZ_ADAPTER1_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Alias");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static guint 
org_bluez_adapter1_proxy_get_class (OrgBluezAdapter1 *object)
{
  OrgBluezAdapter1Proxy *proxy = ORG_BLUEZ_ADAPTER1_PROXY (object);
  GVariant *variant;
  guint value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Class");
  if (variant != NULL)
    {
      value = g_variant_get_uint32 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean 
org_bluez_adapter1_proxy_get_powered (OrgBluezAdapter1 *object)
{
  OrgBluezAdapter1Proxy *proxy = ORG_BLUEZ_ADAPTER1_PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Powered");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean 
org_bluez_adapter1_proxy_get_discoverable (OrgBluezAdapter1 *object)
{
  OrgBluezAdapter1Proxy *proxy = ORG_BLUEZ_ADAPTER1_PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Discoverable");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static guint 
org_bluez_adapter1_proxy_get_discoverable_timeout (OrgBluezAdapter1 *object)
{
  OrgBluezAdapter1Proxy *proxy = ORG_BLUEZ_ADAPTER1_PROXY (object);
  GVariant *variant;
  guint value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "DiscoverableTimeout");
  if (variant != NULL)
    {
      value = g_variant_get_uint32 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean 
org_bluez_adapter1_proxy_get_pairable (OrgBluezAdapter1 *object)
{
  OrgBluezAdapter1Proxy *proxy = ORG_BLUEZ_ADAPTER1_PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Pairable");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static guint 
org_bluez_adapter1_proxy_get_pairable_timeout (OrgBluezAdapter1 *object)
{
  OrgBluezAdapter1Proxy *proxy = ORG_BLUEZ_ADAPTER1_PROXY (object);
  GVariant *variant;
  guint value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "PairableTimeout");
  if (variant != NULL)
    {
      value = g_variant_get_uint32 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean 
org_bluez_adapter1_proxy_get_discovering (OrgBluezAdapter1 *object)
{
  OrgBluezAdapter1Proxy *proxy = ORG_BLUEZ_ADAPTER1_PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Discovering");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *const *
org_bluez_adapter1_proxy_get_uuids (OrgBluezAdapter1 *object)
{
  OrgBluezAdapter1Proxy *proxy = ORG_BLUEZ_ADAPTER1_PROXY (object);
  GVariant *variant;
  const gchar *const *value = NULL;
  value = g_datalist_get_data (&proxy->priv->qdata, "UUIDs");
  if (value != NULL)
    return value;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "UUIDs");
  if (variant != NULL)
    {
      value = g_variant_get_strv (variant, NULL);
      g_datalist_set_data_full (&proxy->priv->qdata, "UUIDs", (gpointer) value, g_free);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
org_bluez_adapter1_proxy_get_modalias (OrgBluezAdapter1 *object)
{
  OrgBluezAdapter1Proxy *proxy = ORG_BLUEZ_ADAPTER1_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Modalias");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static void
org_bluez_adapter1_proxy_init (OrgBluezAdapter1Proxy *proxy)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  proxy->priv = org_bluez_adapter1_proxy_get_instance_private (proxy);
#else
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, TYPE_ORG_BLUEZ_ADAPTER1_PROXY, OrgBluezAdapter1ProxyPrivate);
#endif

  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), org_bluez_adapter1_interface_info ());
}

static void
org_bluez_adapter1_proxy_class_init (OrgBluezAdapter1ProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = org_bluez_adapter1_proxy_finalize;
  gobject_class->get_property = org_bluez_adapter1_proxy_get_property;
  gobject_class->set_property = org_bluez_adapter1_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = org_bluez_adapter1_proxy_g_signal;
  proxy_class->g_properties_changed = org_bluez_adapter1_proxy_g_properties_changed;

  org_bluez_adapter1_override_properties (gobject_class, 1);

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (OrgBluezAdapter1ProxyPrivate));
#endif
}

static void
org_bluez_adapter1_proxy_iface_init (OrgBluezAdapter1Iface *iface)
{
  iface->get_address = org_bluez_adapter1_proxy_get_address;
  iface->get_name = org_bluez_adapter1_proxy_get_name;
  iface->get_alias = org_bluez_adapter1_proxy_get_alias;
  iface->get_class = org_bluez_adapter1_proxy_get_class;
  iface->get_powered = org_bluez_adapter1_proxy_get_powered;
  iface->get_discoverable = org_bluez_adapter1_proxy_get_discoverable;
  iface->get_discoverable_timeout = org_bluez_adapter1_proxy_get_discoverable_timeout;
  iface->get_pairable = org_bluez_adapter1_proxy_get_pairable;
  iface->get_pairable_timeout = org_bluez_adapter1_proxy_get_pairable_timeout;
  iface->get_discovering = org_bluez_adapter1_proxy_get_discovering;
  iface->get_uuids = org_bluez_adapter1_proxy_get_uuids;
  iface->get_modalias = org_bluez_adapter1_proxy_get_modalias;
}

/**
 * org_bluez_adapter1_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-bluez-Adapter1.top_of_page">org.bluez.Adapter1</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call org_bluez_adapter1_proxy_new_finish() to get the result of the operation.
 *
 * See org_bluez_adapter1_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
org_bluez_adapter1_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_ORG_BLUEZ_ADAPTER1_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.bluez.Adapter1", NULL);
}

/**
 * org_bluez_adapter1_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to org_bluez_adapter1_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with org_bluez_adapter1_proxy_new().
 *
 * Returns: (transfer full) (type OrgBluezAdapter1Proxy): The constructed proxy object or %NULL if @error is set.
 */
OrgBluezAdapter1 *
org_bluez_adapter1_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return ORG_BLUEZ_ADAPTER1 (ret);
  else
    return NULL;
}

/**
 * org_bluez_adapter1_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-bluez-Adapter1.top_of_page">org.bluez.Adapter1</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See org_bluez_adapter1_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type OrgBluezAdapter1Proxy): The constructed proxy object or %NULL if @error is set.
 */
OrgBluezAdapter1 *
org_bluez_adapter1_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_ORG_BLUEZ_ADAPTER1_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.bluez.Adapter1", NULL);
  if (ret != NULL)
    return ORG_BLUEZ_ADAPTER1 (ret);
  else
    return NULL;
}


/**
 * org_bluez_adapter1_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like org_bluez_adapter1_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call org_bluez_adapter1_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See org_bluez_adapter1_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
org_bluez_adapter1_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_ORG_BLUEZ_ADAPTER1_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.bluez.Adapter1", NULL);
}

/**
 * org_bluez_adapter1_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to org_bluez_adapter1_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with org_bluez_adapter1_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type OrgBluezAdapter1Proxy): The constructed proxy object or %NULL if @error is set.
 */
OrgBluezAdapter1 *
org_bluez_adapter1_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return ORG_BLUEZ_ADAPTER1 (ret);
  else
    return NULL;
}

/**
 * org_bluez_adapter1_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like org_bluez_adapter1_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See org_bluez_adapter1_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type OrgBluezAdapter1Proxy): The constructed proxy object or %NULL if @error is set.
 */
OrgBluezAdapter1 *
org_bluez_adapter1_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_ORG_BLUEZ_ADAPTER1_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.bluez.Adapter1", NULL);
  if (ret != NULL)
    return ORG_BLUEZ_ADAPTER1 (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * OrgBluezAdapter1Skeleton:
 *
 * The #OrgBluezAdapter1Skeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * OrgBluezAdapter1SkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #OrgBluezAdapter1Skeleton.
 */

struct _OrgBluezAdapter1SkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_org_bluez_adapter1_skeleton_handle_method_call (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  OrgBluezAdapter1Skeleton *skeleton = ORG_BLUEZ_ADAPTER1_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  guint num_extra;
  gsize n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], TYPE_ORG_BLUEZ_ADAPTER1);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_ORG_BLUEZ_ADAPTER1);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_org_bluez_adapter1_skeleton_handle_get_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  OrgBluezAdapter1Skeleton *skeleton = ORG_BLUEZ_ADAPTER1_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_org_bluez_adapter1_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_org_bluez_adapter1_skeleton_handle_set_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  OrgBluezAdapter1Skeleton *skeleton = ORG_BLUEZ_ADAPTER1_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_org_bluez_adapter1_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _org_bluez_adapter1_skeleton_vtable =
{
  _org_bluez_adapter1_skeleton_handle_method_call,
  _org_bluez_adapter1_skeleton_handle_get_property,
  _org_bluez_adapter1_skeleton_handle_set_property,
  {NULL}
};

static GDBusInterfaceInfo *
org_bluez_adapter1_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return org_bluez_adapter1_interface_info ();
}

static GDBusInterfaceVTable *
org_bluez_adapter1_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return (GDBusInterfaceVTable *) &_org_bluez_adapter1_skeleton_vtable;
}

static GVariant *
org_bluez_adapter1_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  OrgBluezAdapter1Skeleton *skeleton = ORG_BLUEZ_ADAPTER1_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_org_bluez_adapter1_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _org_bluez_adapter1_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _org_bluez_adapter1_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _org_bluez_adapter1_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.bluez.Adapter1", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static gboolean _org_bluez_adapter1_emit_changed (gpointer user_data);

static void
org_bluez_adapter1_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
  OrgBluezAdapter1Skeleton *skeleton = ORG_BLUEZ_ADAPTER1_SKELETON (_skeleton);
  gboolean emit_changed = FALSE;

  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    {
      g_source_destroy (skeleton->priv->changed_properties_idle_source);
      skeleton->priv->changed_properties_idle_source = NULL;
      emit_changed = TRUE;
    }
  g_mutex_unlock (&skeleton->priv->lock);

  if (emit_changed)
    _org_bluez_adapter1_emit_changed (skeleton);
}

static void org_bluez_adapter1_skeleton_iface_init (OrgBluezAdapter1Iface *iface);
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (OrgBluezAdapter1Skeleton, org_bluez_adapter1_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_ADD_PRIVATE (OrgBluezAdapter1Skeleton)
                         G_IMPLEMENT_INTERFACE (TYPE_ORG_BLUEZ_ADAPTER1, org_bluez_adapter1_skeleton_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (OrgBluezAdapter1Skeleton, org_bluez_adapter1_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (TYPE_ORG_BLUEZ_ADAPTER1, org_bluez_adapter1_skeleton_iface_init))

#endif
static void
org_bluez_adapter1_skeleton_finalize (GObject *object)
{
  OrgBluezAdapter1Skeleton *skeleton = ORG_BLUEZ_ADAPTER1_SKELETON (object);
  guint n;
  for (n = 0; n < 12; n++)
    g_value_unset (&skeleton->priv->properties[n]);
  g_free (skeleton->priv->properties);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (org_bluez_adapter1_skeleton_parent_class)->finalize (object);
}

static void
org_bluez_adapter1_skeleton_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  OrgBluezAdapter1Skeleton *skeleton = ORG_BLUEZ_ADAPTER1_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 12);
  g_mutex_lock (&skeleton->priv->lock);
  g_value_copy (&skeleton->priv->properties[prop_id - 1], value);
  g_mutex_unlock (&skeleton->priv->lock);
}

static gboolean
_org_bluez_adapter1_emit_changed (gpointer user_data)
{
  OrgBluezAdapter1Skeleton *skeleton = ORG_BLUEZ_ADAPTER1_SKELETON (user_data);
  GList *l;
  GVariantBuilder builder;
  GVariantBuilder invalidated_builder;
  guint num_changes;

  g_mutex_lock (&skeleton->priv->lock);
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  g_variant_builder_init (&invalidated_builder, G_VARIANT_TYPE ("as"));
  for (l = skeleton->priv->changed_properties, num_changes = 0; l != NULL; l = l->next)
    {
      ChangedProperty *cp = l->data;
      GVariant *variant;
      const GValue *cur_value;

      cur_value = &skeleton->priv->properties[cp->prop_id - 1];
      if (!_g_value_equal (cur_value, &cp->orig_value))
        {
          variant = g_dbus_gvalue_to_gvariant (cur_value, G_VARIANT_TYPE (cp->info->parent_struct.signature));
          g_variant_builder_add (&builder, "{sv}", cp->info->parent_struct.name, variant);
          g_variant_unref (variant);
          num_changes++;
        }
    }
  if (num_changes > 0)
    {
      GList *connections, *ll;
      GVariant *signal_variant;
      signal_variant = g_variant_ref_sink (g_variant_new ("(sa{sv}as)", "org.bluez.Adapter1",
                                           &builder, &invalidated_builder));
      connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));
      for (ll = connections; ll != NULL; ll = ll->next)
        {
          GDBusConnection *connection = ll->data;

          g_dbus_connection_emit_signal (connection,
                                         NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)),
                                         "org.freedesktop.DBus.Properties",
                                         "PropertiesChanged",
                                         signal_variant,
                                         NULL);
        }
      g_variant_unref (signal_variant);
      g_list_free_full (connections, g_object_unref);
    }
  else
    {
      g_variant_builder_clear (&builder);
      g_variant_builder_clear (&invalidated_builder);
    }
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  skeleton->priv->changed_properties = NULL;
  skeleton->priv->changed_properties_idle_source = NULL;
  g_mutex_unlock (&skeleton->priv->lock);
  return FALSE;
}

static void
_org_bluez_adapter1_schedule_emit_changed (OrgBluezAdapter1Skeleton *skeleton, const _ExtendedGDBusPropertyInfo *info, guint prop_id, const GValue *orig_value)
{
  ChangedProperty *cp;
  GList *l;
  cp = NULL;
  for (l = skeleton->priv->changed_properties; l != NULL; l = l->next)
    {
      ChangedProperty *i_cp = l->data;
      if (i_cp->info == info)
        {
          cp = i_cp;
          break;
        }
    }
  if (cp == NULL)
    {
      cp = g_new0 (ChangedProperty, 1);
      cp->prop_id = prop_id;
      cp->info = info;
      skeleton->priv->changed_properties = g_list_prepend (skeleton->priv->changed_properties, cp);
      g_value_init (&cp->orig_value, G_VALUE_TYPE (orig_value));
      g_value_copy (orig_value, &cp->orig_value);
    }
}

static void
org_bluez_adapter1_skeleton_notify (GObject      *object,
  GParamSpec *pspec G_GNUC_UNUSED)
{
  OrgBluezAdapter1Skeleton *skeleton = ORG_BLUEZ_ADAPTER1_SKELETON (object);
  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties != NULL &&
      skeleton->priv->changed_properties_idle_source == NULL)
    {
      skeleton->priv->changed_properties_idle_source = g_idle_source_new ();
      g_source_set_priority (skeleton->priv->changed_properties_idle_source, G_PRIORITY_DEFAULT);
      g_source_set_callback (skeleton->priv->changed_properties_idle_source, _org_bluez_adapter1_emit_changed, g_object_ref (skeleton), (GDestroyNotify) g_object_unref);
      g_source_set_name (skeleton->priv->changed_properties_idle_source, "[generated] _org_bluez_adapter1_emit_changed");
      g_source_attach (skeleton->priv->changed_properties_idle_source, skeleton->priv->context);
      g_source_unref (skeleton->priv->changed_properties_idle_source);
    }
  g_mutex_unlock (&skeleton->priv->lock);
}

static void
org_bluez_adapter1_skeleton_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
  const _ExtendedGDBusPropertyInfo *info;
  OrgBluezAdapter1Skeleton *skeleton = ORG_BLUEZ_ADAPTER1_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 12);
  info = (const _ExtendedGDBusPropertyInfo *) _org_bluez_adapter1_property_info_pointers[prop_id - 1];
  g_mutex_lock (&skeleton->priv->lock);
  g_object_freeze_notify (object);
  if (!_g_value_equal (value, &skeleton->priv->properties[prop_id - 1]))
    {
      if (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)) != NULL &&
          info->emits_changed_signal)
        _org_bluez_adapter1_schedule_emit_changed (skeleton, info, prop_id, &skeleton->priv->properties[prop_id - 1]);
      g_value_copy (value, &skeleton->priv->properties[prop_id - 1]);
      g_object_notify_by_pspec (object, pspec);
    }
  g_mutex_unlock (&skeleton->priv->lock);
  g_object_thaw_notify (object);
}

static void
org_bluez_adapter1_skeleton_init (OrgBluezAdapter1Skeleton *skeleton)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  skeleton->priv = org_bluez_adapter1_skeleton_get_instance_private (skeleton);
#else
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, TYPE_ORG_BLUEZ_ADAPTER1_SKELETON, OrgBluezAdapter1SkeletonPrivate);
#endif

  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
  skeleton->priv->properties = g_new0 (GValue, 12);
  g_value_init (&skeleton->priv->properties[0], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[1], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[2], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[3], G_TYPE_UINT);
  g_value_init (&skeleton->priv->properties[4], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[5], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[6], G_TYPE_UINT);
  g_value_init (&skeleton->priv->properties[7], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[8], G_TYPE_UINT);
  g_value_init (&skeleton->priv->properties[9], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[10], G_TYPE_STRV);
  g_value_init (&skeleton->priv->properties[11], G_TYPE_STRING);
}

static const gchar *
org_bluez_adapter1_skeleton_get_address (OrgBluezAdapter1 *object)
{
  OrgBluezAdapter1Skeleton *skeleton = ORG_BLUEZ_ADAPTER1_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[0]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
org_bluez_adapter1_skeleton_get_name (OrgBluezAdapter1 *object)
{
  OrgBluezAdapter1Skeleton *skeleton = ORG_BLUEZ_ADAPTER1_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[1]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
org_bluez_adapter1_skeleton_get_alias (OrgBluezAdapter1 *object)
{
  OrgBluezAdapter1Skeleton *skeleton = ORG_BLUEZ_ADAPTER1_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[2]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static guint 
org_bluez_adapter1_skeleton_get_class (OrgBluezAdapter1 *object)
{
  OrgBluezAdapter1Skeleton *skeleton = ORG_BLUEZ_ADAPTER1_SKELETON (object);
  guint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_uint (&(skeleton->priv->properties[3]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean 
org_bluez_adapter1_skeleton_get_powered (OrgBluezAdapter1 *object)
{
  OrgBluezAdapter1Skeleton *skeleton = ORG_BLUEZ_ADAPTER1_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[4]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean 
org_bluez_adapter1_skeleton_get_discoverable (OrgBluezAdapter1 *object)
{
  OrgBluezAdapter1Skeleton *skeleton = ORG_BLUEZ_ADAPTER1_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[5]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static guint 
org_bluez_adapter1_skeleton_get_discoverable_timeout (OrgBluezAdapter1 *object)
{
  OrgBluezAdapter1Skeleton *skeleton = ORG_BLUEZ_ADAPTER1_SKELETON (object);
  guint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_uint (&(skeleton->priv->properties[6]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean 
org_bluez_adapter1_skeleton_get_pairable (OrgBluezAdapter1 *object)
{
  OrgBluezAdapter1Skeleton *skeleton = ORG_BLUEZ_ADAPTER1_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[7]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static guint 
org_bluez_adapter1_skeleton_get_pairable_timeout (OrgBluezAdapter1 *object)
{
  OrgBluezAdapter1Skeleton *skeleton = ORG_BLUEZ_ADAPTER1_SKELETON (object);
  guint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_uint (&(skeleton->priv->properties[8]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean 
org_bluez_adapter1_skeleton_get_discovering (OrgBluezAdapter1 *object)
{
  OrgBluezAdapter1Skeleton *skeleton = ORG_BLUEZ_ADAPTER1_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[9]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *const *
org_bluez_adapter1_skeleton_get_uuids (OrgBluezAdapter1 *object)
{
  OrgBluezAdapter1Skeleton *skeleton = ORG_BLUEZ_ADAPTER1_SKELETON (object);
  const gchar *const *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boxed (&(skeleton->priv->properties[10]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
org_bluez_adapter1_skeleton_get_modalias (OrgBluezAdapter1 *object)
{
  OrgBluezAdapter1Skeleton *skeleton = ORG_BLUEZ_ADAPTER1_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[11]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static void
org_bluez_adapter1_skeleton_class_init (OrgBluezAdapter1SkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = org_bluez_adapter1_skeleton_finalize;
  gobject_class->get_property = org_bluez_adapter1_skeleton_get_property;
  gobject_class->set_property = org_bluez_adapter1_skeleton_set_property;
  gobject_class->notify       = org_bluez_adapter1_skeleton_notify;


  org_bluez_adapter1_override_properties (gobject_class, 1);

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = org_bluez_adapter1_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = org_bluez_adapter1_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = org_bluez_adapter1_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = org_bluez_adapter1_skeleton_dbus_interface_get_vtable;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (OrgBluezAdapter1SkeletonPrivate));
#endif
}

static void
org_bluez_adapter1_skeleton_iface_init (OrgBluezAdapter1Iface *iface)
{
  iface->get_address = org_bluez_adapter1_skeleton_get_address;
  iface->get_name = org_bluez_adapter1_skeleton_get_name;
  iface->get_alias = org_bluez_adapter1_skeleton_get_alias;
  iface->get_class = org_bluez_adapter1_skeleton_get_class;
  iface->get_powered = org_bluez_adapter1_skeleton_get_powered;
  iface->get_discoverable = org_bluez_adapter1_skeleton_get_discoverable;
  iface->get_discoverable_timeout = org_bluez_adapter1_skeleton_get_discoverable_timeout;
  iface->get_pairable = org_bluez_adapter1_skeleton_get_pairable;
  iface->get_pairable_timeout = org_bluez_adapter1_skeleton_get_pairable_timeout;
  iface->get_discovering = org_bluez_adapter1_skeleton_get_discovering;
  iface->get_uuids = org_bluez_adapter1_skeleton_get_uuids;
  iface->get_modalias = org_bluez_adapter1_skeleton_get_modalias;
}

/**
 * org_bluez_adapter1_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-org-bluez-Adapter1.top_of_page">org.bluez.Adapter1</link>.
 *
 * Returns: (transfer full) (type OrgBluezAdapter1Skeleton): The skeleton object.
 */
OrgBluezAdapter1 *
org_bluez_adapter1_skeleton_new (void)
{
  return ORG_BLUEZ_ADAPTER1 (g_object_new (TYPE_ORG_BLUEZ_ADAPTER1_SKELETON, NULL));
}

